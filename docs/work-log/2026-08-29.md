# Work Log — 2026-08-29

## Public landing-page review and C30 campaign listing

### Objective

- Review the live Drago Russo Games landing page and its source architecture.
- Preserve the accepted hero design.
- Replace the `Cinematic Worlds` Table Experience pillar with `Heavy Roleplay`.
- Add active campaign C30 Hacienda Last Light with its shared Google Sheet.
- Audit the landing-page design and code without changing unrelated services.

### Scope and baseline

- Public static website only: repository-root `index.html` and supporting
  landing-page assets/styles.
- No React portal, backend, database, migration, Discord bot, Foundry world,
  Cloudflare configuration, or DigitalOcean/VPS mutation is authorized.
- Branch: `codex/first-edition-mapping`.
- Baseline commit: `39f378e5c6ffeec5484e10780028c05ca02cd499`.
- Upstream: `origin/codex/first-edition-mapping` at the same commit after
  `git fetch origin --prune`.
- Pre-existing worktree changes include unrelated backend, frontend, mapping,
  follower, character-vault, private-reference, temporary, Wrangler, migration,
  test, and documentation work. They are preserved and excluded from this
  landing-page scope.

### Architecture reviewed

- The root public website is a static Cloudflare Pages deployment.
- `frontend/` is the separate React/Vite Drago Table application.
- `backend/` is the separate FastAPI/SQLAlchemy/Alembic service with PostgreSQL
  persistence in the documented DigitalOcean/VPS topology.
- DM, Classic player, API, public website, and Foundry endpoints remain separate
  operational and deployment surfaces.

### Implementation

- Replaced `Cinematic Worlds` with `Heavy Roleplay` and added the accepted
  explanation that the world and NPCs feel alive while words and actions carry
  lasting consequences.
- Added the active C30 Hacienda Last Light campaign card for Tuesday at 11:00
  AM GMT-5, linked to the controlled shared Google Sheet in a new tab.
- Added the approved 1120×630 Hacienda StartPlaying banner from the controlled
  Good Society Caribbean folder as the C30 campaign artwork.
- Centered the tenth active-campaign card on three-column desktop layouts while
  returning it to the normal single column on narrower layouts.

### Verification

- `git diff --check` passed.
- Browser review passed for the accepted hero, updated Table Experience, active
  campaign grid, and centered C30 card.
- A true 390-pixel mobile viewport reported a 390-pixel document width with no
  horizontal overflow. The responsive single-column rule remains active below
  900 pixels.
- All 32 relative `href` and `src` references in the landing page resolve to
  existing local files or directories.
- The C30 link opens the authenticated Google Sheet currently titled
  `Hacienda Lost Light` in the existing owner browser session. An anonymous HTTP request
  receives 401, so campaign access depends on Google sign-in and the Sheet's
  sharing list.
- Public checks returned HTTP 200 for the root site, `www`, DM portal, Classic
  portal, and shared API health endpoint.
- No temporary preview or diagnostic file remains in the repository.

### Deployment

- The user authorized production publication after reviewing the Hacienda artwork.
- Fast-forwarded the exact approved commit,
  `3dc47f143e47019b9fa80e36d8b51c699a0fee3c`, to `origin/main` without a force
  push. Both the working branch and production branch point to that commit.
- Built a clean 999-file, 49 MB public snapshot from that exact commit. HTML
  parsing and JavaScript syntax checks passed; no file exceeds 20 MB.
- The package excludes backend, frontend application, Discord bot,
  documentation, tests, tools, packaging, scripts, private-reference, and
  source-rulebook paths. Only the two approved public character-sheet PDFs are
  present.
- Published the package to the production Cloudflare Pages project
  `dragorussogames-web` as branch `main`.
- Immutable deployment: `https://cacbebb1.dragorussogames-web.pages.dev`.
- Verified the approved `Heavy Roleplay` copy, Hacienda Last Light card, exact
  stylesheet hash, and exact Hacienda artwork hash on the immutable deployment
  and custom domains. Root, `www`, DM, Classic, and API health checks passed.
- Representative private-reference, backend, documentation, and source-PDF
  probes returned only the public HTML fallback. No VPS, backend, database,
  migration, Foundry, module, tunnel, DM portal, or Classic portal mutation was
  performed.
