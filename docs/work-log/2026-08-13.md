# Work Log — 2026-08-13

## Foundry JSON character viewer

### Objective

- Build a polished public character-sheet viewer that accepts a Foundry VTT
  Actor JSON export and automatically presents either an ARS/OSRIC or D&D 5e
  character as a compact, readable, printable sheet.
- Make the experience work well on phones while retaining the visual character
  of a sheet a player could print and bring to the table.

### Intended scope

- A new, standalone static public utility under `character-viewer/`.
- Browser-only file reading and parsing. Character JSON must not be uploaded,
  persisted to the backend, or written into the existing character vault.
- Separate visual treatments and data adapters for ARS/OSRIC and Foundry D&D
  5e/SRD 5.2 exports, behind one import experience.
- No backend, database, Drago Table, Foundry world, Foundry module, existing
  character builder, deployment, or live-service changes.

### Baseline

- Branch: `codex/first-edition-mapping`.
- Baseline commit: `468aaa93d870ceafceedf9c4f65e5d84ce60cd2c`.
- Locally recorded upstream divergence: `0 ahead / 0 behind`.
- `git fetch origin --prune` was attempted, but the managed workspace denied
  writing `.git/FETCH_HEAD`; remote-tracking references were not updated.

### Pre-existing changes

- The worktree already contains unrelated backend, follower, Drago Table,
  mapping, character-vault, test, progress-log, deployment-record, private
  reference, temporary, and Wrangler changes.
- These changes remain outside this feature's scope and must not be overwritten,
  staged, or silently absorbed.

### Research and decisions

- Reviewed `docs/work-log/PROGRESS.md`, the latest dated work log
  (`2026-08-01.md`), the Classic portal architecture note, and the existing
  character vault/sheet entry points.
- Inspected real local exports from both systems:
  - ARS: `fvtt-Actor-testigio-PZ0Q3bVKClmyir5H.json`;
  - Foundry D&D 5e: `fvtt-Actor-leopold-von-stiehl-hpXYhMeG9lQHn9Jr.json`
    and `fvtt-Actor-leopold-von-stiehl-VBAIC36kzxLYjqfE.json`.
- Confirmed the systems require separate adapters: ARS uses THAC0, descending
  AC, ten classic saves, memorization data, and ARS item types; D&D 5e uses
  proficiency-based abilities, modern saves and skills, classes/backgrounds,
  spell slots, activities, and 5e item types.
- Product boundary: the first release is a private read-only viewer, not an
  import into the account-based Drago builder. This avoids creating a second
  character builder and protects player data.

### Planned verification

- Import and render the real ARS and D&D 5e sample exports.
- Verify automatic system recognition and graceful unsupported/invalid JSON
  errors.
- Verify keyboard-accessible upload/drop interactions.
- Verify no network/API calls are made by the utility.
- Inspect desktop, phone, and print layouts visually.
- Run JavaScript syntax checks, HTML parsing, and `git diff --check`.

### Implementation

- Added `character-viewer/index.html`, `character-viewer/viewer.css`, and
  `character-viewer/viewer.js`.
- Built a single drag/drop, file-picker, or pasted-JSON entry experience with
  automatic recognition of ARS/OSRIC and Foundry D&D 5e character actors.
- Kept character data entirely in the browser; the viewer makes no character
  upload, persistence, backend, or account-vault request.
- Created a compact parchment-and-ink sheet with distinct classic crimson and
  modern blue identities, dense table-ready panels, responsive phone layout,
  and print/PDF styles sized for a paper character sheet.
- Added system-specific presentation for classic THAC0, descending AC, ten
  saves, rate/range, weapon speed, and proficiencies, plus modern proficiency,
  saves, skills, mastery, spells, and multiclass levels.
- Normalized ARS abbreviated alignments, class-level fallback, base movement,
  level-appropriate saves, and fractional weights. The modern adapter also
  reads species movement and hides unresolved Foundry scale expressions.

### Verification completed

- JavaScript syntax check passed and the HTML parsed successfully.
- `git diff --check` passed for the new viewer and this work log.
- Searched the feature for `fetch`, `XMLHttpRequest`, `sendBeacon`, and
  `WebSocket`; no character-data network path exists.
- Imported and rendered the real Testigio ARS/OSRIC export successfully.
- Imported and rendered the real Leopold D&D 5e multiclass export
  successfully, including Fighter 5 / Sorcerer 3, armor, skills, weapons,
  spells, and features.
- Browser console check completed with no warnings or errors.
- Visually inspected desktop classic and modern sheets and a 390 × 844 phone
  layout. The phone layout stacks the paper sections cleanly and retains
  legible controls and statistics.
- Confirmed responsive speed fallback (`30 ft`) for the real 5e species export
  and the corrected ARS identity, movement, and compact fractional weight.

### Status / next action

- The viewer is complete locally. Publishing was approved in this session.
- Publishing scope remains frontend-only: add a discoverable homepage link,
  commit and push only the viewer/homepage/recordkeeping files, package the
  complete public site from the exact commit, and deploy it to the
  `dragorussogames-web` Cloudflare Pages project.
- No backend, database, migration, VPS service, Foundry world, or tunnel change
  is required or authorized for this release.
- Deployment verification will cover the live homepage link and the public
  `/character-viewer/` route. Results and the exact deployment identity will be
  recorded below as they occur.

### Publication completed

- Added a `Character Viewer` button to the public homepage hero.
- Committed the exact six-file release as
  `c0712c31b672a089a4378f2794ae96027cb2043d` and pushed it to
  `origin/codex/first-edition-mapping`.
- GitHub initially returned transient internal errors while fast-forwarding
  `origin/main`. A later non-force retry succeeded, placing the exact deployed
  commit on `main`; no force push was used.
- Built a fresh public snapshot from the exact commit. The final package has
  999 files, is 48 MB, contains no file over 20 MB, and excludes backend,
  private-reference, documentation, tests, and private/source rulebooks.
- Published the package directly to the production `dragorussogames-web`
  Cloudflare Pages project as branch `main`.
- Deployment URL: `https://a34ecc0a.dragorussogames-web.pages.dev`.
- Verified the homepage link, viewer HTML, CSS, JavaScript, and logo on
  `https://www.dragorussogames.com/` and
  `https://www.dragorussogames.com/character-viewer/`.
- Confirmed representative private-reference and backend probe paths returned
  HTML fallbacks rather than source files.
- No backend, database, migration, VPS, Foundry, or tunnel change was made.

## Classic-sheet table-use refinement

### Objective and scope

- Refine only the public browser-based Character Viewer’s ARS/OSRIC sheet after
  reviewing the first live export with the user.
- Present multiclass levels clearly, compact the attack rows, separate rate
  from range, show exported attack bonuses, translate skill targets into their
  useful percentile or chance-on-d6 form, suppress Wrestle/Tackle, and show an
  encumbrance summary when that data exists in the Foundry export.
- Keep the viewer labelled as OSRIC rather than presenting ARS as a player-facing
  rules identity. No backend, database, VPS, Foundry-world, module, or character
  builder change is in scope.

### Baseline and repository state

- Branch: `codex/first-edition-mapping`.
- Baseline commit: `df52844b5f49cf0900d7b6918fb41f0c0183b56d`.
- Upstream state after `git fetch origin --prune`: `0 ahead / 0 behind`.
- The pre-existing unrelated backend, follower, Drago Table, mapping,
  character-vault, test, progress-log, deployment-record, private-reference,
  temporary, and Wrangler changes remain present and outside this correction.

### Planned verification

- Inspect the actual John Regis Smith Actor JSON fields before changing the
  adapter.
- Render both single-class and multiclass OSRIC exports.
- Check attack bonus, rate/range, proficiency target, encumbrance, phone, and
  print presentation.
- Run JavaScript syntax, HTML parse, `git diff --check`, and browser console
  checks before any publication decision.

### Implementation completed

- Reduced character-sheet typography, cards, spacing, and table rows by
  approximately twenty percent for both screen and print.
- Reworked attack rows so Rate and Range occupy separate compact lines with
  sufficient width.
- Read ARS weapon action groups and split a thrown dagger into distinct
  `Dagger (Melee)` and `Dagger (Thrown)` attacks when both actions exist in the
  Foundry export.
- Preserved attack bonuses through each weapon's effective THAC0 value.
- Standardized visible OSRIC checks as percentages or `x in d6` values in both
  the live viewer and print output.
- Replaced heavy or dashed printed row rules with subtle tan separators.
- Added a purpose-built three-page print layout: primary statistics and combat
  on page one; checks, equipment, and features on page two; spells, coin, and a
  full-height notes area on page three.
- Removed the continuation-page outer border and extended the third-page notes
  area to the bottom of the usable page.

### Verification completed

- `node --check character-viewer/viewer.js` passed.
- The HTML parser check for `character-viewer/index.html` passed.
- `git diff --check` passed for the scoped viewer and log files.
- Loaded the real `fvtt-Actor-john-regis-smith-1XLF3zoao2MTCFML.json` export in
  the browser and confirmed separate melee/thrown dagger attacks, no weapon-row
  horizontal overflow, and visible percentage and d6 check values.
- Rendered the real character through Chromium print-to-PDF as US Letter:
  exactly three pages, all visually inspected, with no clipped content and a
  full-height third-page notes area.

### Status / next action

- Implementation and local verification are complete.
- This correction has not yet been committed, pushed, or deployed;
  publication remains a separate requested action.

### Implementation completed

- Replaced the single numeric level seal with a compact class-level list so a
  multiclass export can show each class and its own level without collapsing
  the character into one misleading number.
- Tightened the OSRIC attack rows, kept THAC0 on one line, separated Rate from
  Range, and added support for an exported weapon attack modifier or magic
  bonus such as `+1 to hit`.
- Converted exported `1d100` proficiency targets to percentages and `1d6`
  targets to chance-on-d6 notation. Removed the redundant Base Movement entry
  and the unused Wrestle/Tackle entry from the printed check list.
- Added carried load as an Encumbrance quick statistic by totaling the
  quantities and weights actually present in the Foundry Actor export. The
  export does not contain a ready-made encumbrance category, so the viewer does
  not invent one.
- Changed player-facing classic labels from ARS/OSRIC to OSRIC while retaining
  the ARS data adapter internally.
- Added a full-width `Character Sheet · Continued` masthead to the printed
  reference page. It repeats the character name and identity before Spells,
  Coin, and Notes so page two no longer begins as an unlabelled, cut-off
  continuation.

### Verification completed

- Rendered the real John Regis Smith Foundry Actor export in the local browser.
  Confirmed OSRIC labelling, Thief Level 1, 93.2 lb carried load, compact weapon
  rows, percentage/d6 checks, and removal of Wrestle/Tackle and Base Movement.
- Confirmed the real export stores only raw STR/DEX scores, while weapon attack
  modifiers and magic bonuses live on the weapon items; the viewer now reads
  those item fields and displays non-zero bonuses.
- JavaScript syntax checking and `git diff --check` passed for the viewer and
  this work log.

### Status / next action

- The refinements are complete locally and publication has been approved.
- Release scope is limited to the three Character Viewer files and the
  corresponding work/deployment records. No backend or Drago Table files will
  be staged or deployed.

### Refinement publication completed

- Committed the exact Character Viewer refinement as
  `5361da42583aa7a1316cb7b8d360cb0b6e94579e`, pushed it to
  `origin/codex/first-edition-mapping`, and fast-forwarded the same commit to
  `origin/main` without a force push.
- Created a fresh 999-file, 48 MB Cloudflare Pages package by applying only the
  three committed viewer files to the previously verified clean public
  snapshot. No backend, private-reference, documentation, test, or source
  rulebook material entered the package.
- Published the exact package to the production `dragorussogames-web` project
  as branch `main`. Deployment URL:
  `https://f0777154.dragorussogames-web.pages.dev`.
- Verified HTTP 200 responses for the viewer HTML, CSS, and JavaScript on both
  the deployment URL and `https://www.dragorussogames.com/character-viewer/`.
- Confirmed the live assets include the OSRIC label, encumbrance summary,
  multiclass-ready level display, compact attack layout, and full-width
  `Character Sheet · Continued` print masthead.
- Representative private-reference and backend URLs returned only the public
  HTML fallback, not private or application source content.

## Compact three-page OSRIC sheet correction

### Objective and scope

- Refine only the public Character Viewer after reviewing the latest on-screen
  and printed John Regis Smith sheet.
- Reduce the sheet typography and spacing by approximately twenty percent,
  give Rate and Range enough room, expose both melee and thrown dagger modes,
  keep classic checks in percentage or chance-on-d6 form on screen and in
  print, remove harsh printed divider lines, and make the continued sheet a
  clean third page with a full-page notes area.
- No backend, database, VPS, Foundry world, module, Drago Table, or character
  builder change is in scope.

### Baseline and repository state

- Branch: `codex/first-edition-mapping`.
- Baseline commit: `e879cadcef1892a6548bf1656da69abca4e901b9`.
- Upstream state after `git fetch origin --prune`: `0 ahead / 0 behind`.
- The pre-existing unrelated backend, follower, Drago Table, mapping,
  character-vault, test, progress-log, deployment-record, private-reference,
  temporary, and Wrangler changes remain present and outside this correction.

### Planned verification

- Inspect the real exported dagger data before adapting its attack modes.
- Render the real John Regis Smith export and inspect the live viewer at desktop
  width.
- Print to PDF and confirm a compact, complete three-page result with no clipped
  Rate/Range values and a third-page notes area extending to the page bottom.
- Run JavaScript syntax, HTML parse, `git diff --check`, and browser console
  checks before any publication decision.

## Dense typography and records-layout correction

### Objective and scope

- Make the Character Viewer substantially denser after the remaining large
  type and oversized stat boxes were identified on screen and in print.
- Reduce sheet typography, numeric callouts, padding, and row heights by about
  another twenty-five percent; keep weapon names, THAC0, damage, speed, Rate,
  and Range readable without collisions.
- Place Class & Racial Features beside Equipment in the screen records area,
  with Notes & Details directly beneath them. Preserve the dedicated full-page
  Notes & Details treatment on printed page three.
- Keep this correction limited to the public Character Viewer and this log. No
  backend, VPS, Foundry-world, Drago Table, or character-builder work is in
  scope.

### Baseline and repository state

- Branch: `codex/first-edition-mapping`.
- Baseline commit: `06fc9ad` (`Refine character viewer print layout`).
- Upstream state after `git fetch origin --prune`: one local commit ahead and
  zero behind.
- The user's screenshots still show the previously published build: they do
  not contain the melee/thrown dagger split already present in the baseline
  commit. This pass is being made against the newer local implementation and
  will be verified before any later publication decision.
- All pre-existing unrelated backend, follower, Drago Table, mapping,
  character-vault, test, progress-log, deployment-record, private-reference,
  temporary, and Wrangler changes remain outside this correction.

### Planned verification

- Render the real John Regis Smith Foundry Actor export at desktop width and
  inspect the weapon rows and new Equipment/Features/Notes arrangement.
- Print to PDF and confirm exactly three compact pages, readable Rate/Range
  values, smaller statistics, Equipment and Features side by side on the
  records page, and a full-height notes area on page three.
- Run JavaScript syntax, HTML parse, `git diff --check`, and browser console
  checks before handoff.

### Implementation completed

- Reduced the screen and print typography, stat values, masthead, panel
  headings, row heights, padding, and gaps throughout the character sheet. The
  visible sheet is now substantially denser rather than merely scaled around
  the same oversized boxes.
- Rebuilt each attack row as a compact single-line record. Weapon names and
  THAC0 now use the same small information hierarchy as Damage and Speed, while
  Rate and Range retain a wider final column.
- Reorganized the screen records area so Equipment and Class & Racial Features
  sit side by side. Spells and Coin follow beneath them, and Notes & Details is
  a full-width section below the records.
- Reorganized printed page two into three columns for Checks & Proficiencies,
  Equipment, and Class & Racial Features. Printed page three remains a clean,
  full-height Notes & Details page with no outer continuation border.

### Verification completed

- Loaded the real John Regis Smith Foundry Actor export in the local browser.
  Confirmed all six attack rows render, including separate `Dagger (Melee)` and
  `Dagger (Thrown)` entries.
- Measured every visible attack row at desktop width. None has horizontal or
  vertical overflow; Rate and Range remain fully visible.
- Confirmed Equipment and Class & Racial Features share the same screen row and
  Notes & Details begins beneath the complete records area.
- Generated and rendered a fresh Letter-size PDF from the real sample export.
  It contains exactly three pages: compact core statistics and attacks on page
  one, the three-column records layout on page two, and the full-height notes
  page on page three. Visual inspection found no clipping, overlap, or broken
  page transition.
- JavaScript syntax, HTML parsing, and `git diff --check` passed. The local
  browser reached a complete ready state, retained the rendered character
  sheet, and reported the expected character-specific page title.
