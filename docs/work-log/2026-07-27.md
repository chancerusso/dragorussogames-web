# Work Log — 2026-07-27

## Multi-class and first-practice readiness

- Objective: complete First Edition multi-class character support before the
  first two practice sessions, then review the complete DM/player journey for
  Wednesday testing and Thursday bug-fix retesting.
- Relevant notes reviewed:
  - `docs/work-log/PROGRESS.md`;
  - `docs/work-log/2026-07-24.md`;
  - `docs/architecture/LOCAL_DRAGO_TABLE_HOST.md`;
  - `docs/drago-table-gameplan.md`.
- Branch: `codex/first-edition-mapping`.
- Baseline and upstream commit after `git fetch origin --prune`:
  `302797e90055553d7c58b5b309217db5790b40d4`.
- Upstream relationship: zero commits ahead and zero behind.
- Pre-existing changes: none.
- Initial finding: the advancement service intentionally exposes a
  multi-class preview shape, but the database has no persistent class-track
  records and the builder, runtime, equipment, spell, advancement, sheet, and
  table summaries still treat `class_name`, `level`, and `xp` as singular.
- Planned scope:
  - define persistent class tracks while preserving every existing
    single-class character;
  - enforce race/combination legality and campaign source boundaries;
  - divide earned XP across class tracks and advance each track separately;
  - derive combat, saves, equipment permissions, spellcasting, and display
    values from the complete class combination;
  - support multi-class creation, editing, sheet presentation, and campaign
    table summaries;
  - retain dual-class as an explicitly separate later feature;
  - run migration, backend, frontend, build, and player-journey verification;
  - produce Wednesday and Thursday practice-session checklists.
- No VPS deployment is authorized or planned.
- Rules verification:
  - checked the repository's private OSRIC reference directly;
  - encoded only the explicitly permitted combinations for Dwarf, Elf, Gnome,
    Half-Elf, Halfling, and Half-Orc;
  - confirmed equal XP division, independent class levels, divided hit-point
    gains, and the race-specific armor/ability restrictions;
  - humans remain single-class at creation; dual-class remains separate.
- Implementation started:
  - added the central multi-class combination and class-track service;
  - added a backward-compatible `class_tracks` JSON field;
  - added migration `0021_multiclass_tracks`, which converts existing
    single-class characters into one-track records without changing them.
- Runtime work:
  - character payloads now expose class and level displays, each track, equal
    XP shares, per-track spell slots, best applicable attack progression, and
    best saving throw per category;
  - race-specific multi-class equipment policy is enforced by the API;
  - level-up preview and application can target an individual class track,
    while divided hit-point gains are retained;
  - the builder now presents only the selected race's legal combinations and
    rolls divided starting hit points across all tracks.
- Verification completed:
  - fresh SQLite migration chain through `0021_multiclass_tracks`: passed;
  - backend/content suite: 163 tests passed;
  - frontend suite: 90 tests passed;
  - production frontend build: passed;
  - Python compilation and `git diff --check`: passed.

## Practice-session plan

### Wednesday — feature rehearsal

1. Start Drago Table locally, sign in as DM, and start remote access.
2. Invite two test player accounts to one practice campaign.
3. Create and assign:
   - an Elf Fighter/Magic-User;
   - a Half-Elf Cleric/Fighter/Magic-User;
   - one existing single-class character as the compatibility control.
4. Confirm each player sees only invited campaigns and only their characters.
5. Verify multi-class HP roll, equal XP shares, class/level display, spells,
   equipment restrictions, saves, THAC0, dice log, marching order, combat
   tracker, map, damage/healing, and reconnect behavior.
6. Record defects with the player, character, screen, action, expected result,
   actual result, and screenshot.

### Thursday — repair and regression

1. Fix Wednesday's reproducible defects in priority order:
   authentication/access, saved data, combat math, then presentation.
2. Re-run migration, backend, frontend, and build checks.
3. Repeat the same characters and scenario so results are comparable.
4. Add one fresh player account and one new multi-class character to ensure
   fixes did not depend on Wednesday's saved records.
5. End only after remote reconnect, save/reload, and single-class compatibility
   all pass.

## Deliberately deferred

- Dual-class remains unavailable and explicit; it is not needed for a new
  first-level character.
- Race level-limit enforcement and an advancement-history audit trail remain
  follow-up hardening. The legal creation combinations and independent
  advancement tracks are implemented now.

## Files changed

- `backend/app/services/multiclass.py`
- `backend/app/db/models.py`
- `backend/alembic/versions/0021_multiclass_tracks.py`
- `backend/app/services/advancement.py`
- `backend/app/api.py`
- `components/character-vault.js`
- `tests/content/test_multiclass_characters.py`
- `tests/content/test_advancement_preview.py`
- `tests/content/test_character_runtime_repairs.py`
- `docs/work-log/PROGRESS.md`
- `docs/work-log/2026-07-27.md`

## Repository record

- Implementation commit:
  `87c1cccceba5f8ae068b51094a68fd4ceb919436`
  (`Add OSRIC multiclass character support`).
- Production deployment was not performed.

## Finder icon refinement

- Objective: reduce the Drago Table artwork inside its macOS icon canvas so
  the dragon/shield no longer appears clipped in Finder.
- Baseline commit: `ea5b241e906f8beaed1982f701c5f43996a30d13`.
- Branch: `codex/first-edition-mapping`; upstream comparison was zero ahead
  and zero behind before this refinement.
- Pre-existing changes: none.
- `git fetch origin --prune` was attempted, but the managed workspace blocked
  writing `.git/FETCH_HEAD`; the already configured upstream comparison
  remained synchronized.
- Planned verification: inspect every ICNS representation, rebuild it with
  consistent transparent inset space, reinstall the local app icon, and
  visually verify the Finder-sized result. No website/VPS deployment.
- Result:
  - extracted all ten ICNS representations and confirmed the original artwork
    touched the canvas edges;
  - reduced the artwork to 82% of each canvas and centered it on transparent
    space without changing the logo;
  - rebuilt `packaging/macos/DragoTable.icns`;
  - installed the new icon into the Desktop `Drago Table.app`, refreshed
    Finder, and re-signed the local app;
  - repository and installed icon SHA-256 values match;
  - ordinary deep code-signature verification passed. Strict verification
    reports Finder/iCloud metadata on the Desktop bundle, which is normal for
    this locally installed app and does not invalidate its ad-hoc signature;
  - `git diff --check` passed.
- Follow-up: Finder continued to show the previous cached appearance after
  installation. Increase the inset from 82% to a clearly visible 72% and
  explicitly refresh macOS icon services before asking for visual acceptance.
- Follow-up result:
  - rebuilt from the original pre-inset icon at 72% scale, avoiding compounded
    resizing;
  - bumped the local macOS bundle build number from 2 to 3 to invalidate the
    old Finder identity;
  - installed the icon and updated Info.plist, re-signed the app, registered
    it again with Launch Services, and restarted icon services and Finder;
  - installed and repository icon hashes match, build number 3 is installed,
    deep signature verification passed, and `git diff --check` passed.
- Second visual follow-up:
  - the latest screenshot confirmed that padding changed, but the dragon still
    looked clipped because the ICNS source itself was a square crop;
  - found the uncropped transparent master at
    `frontend/public/assets/LogoDrago_Mesa_de_trabajo_1.png`;
  - rebuild the icon from the complete master artwork rather than applying
    additional padding to the cropped raster.
- Corrected result:
  - rebuilt the ICNS from the complete transparent master at 78% of the icon
    canvas; the horns, mane, dragon, shield, and surrounding transparent margin
    are now all present;
  - bumped the local build number to 4, installed and re-signed the app,
    registered it with Launch Services, and restarted icon services and Finder;
  - installed and repository icon hashes match, deep signature verification
    passed, and `git diff --check` passed.

## Campaign-authoritative setting options

- Objective: make the DM's campaign setting control all player-visible and
  player-writable character options.
- Baseline commit: `e27e92e7091490bedd6b439741d3211f151bce6b`.
- Branch/upstream: `codex/first-edition-mapping`, zero ahead and zero behind.
- Pre-existing changes: none.
- `git fetch origin --prune` was attempted, but the managed workspace again
  blocked `.git/FETCH_HEAD`; the configured upstream comparison is current.
- Intended behavior:
  - Greyhawk exposes only the OSRIC/First Edition foundation;
  - Dragonlance exposes the shared foundation plus campaign-permitted
    Dragonlance material;
  - players can choose only invited campaigns;
  - the selected campaign filters the builder and reference navigation;
  - backend validation rejects setting-incompatible race and class submissions.
- Planned verification: setting-specific player campaign payloads, builder
  visibility, direct API bypass rejection, existing Greyhawk compatibility,
  frontend tests, backend/content tests, and production build.
- No website/VPS deployment.
- Implemented:
  - Dragonlance campaign payloads now explicitly include both OSRIC foundation
    and Dragonlance material; Greyhawk includes OSRIC/Greyhawk only;
  - Greyhawk builders do not fetch or render Dragonlance race/class sections;
  - Dragonlance builders show the foundation plus Dragonlance additions;
  - campaign switching clears incompatible unsaved Dragonlance selections;
  - player navigation hides Dragonlance unless at least one invited campaign
    uses that setting, and the reference route redirects unauthorized players;
  - backend creation, editing, assignment, and campaign-setting changes reject
    incompatible Dragonlance race/class state;
  - internal DM/runtime compatibility for unassigned legacy Dragonlance
    characters is preserved, while player submissions without a Dragonlance
    campaign remain restricted.
- Verification:
  - focused invitation/setting and multi-class tests: 8 passed;
  - complete backend/content suite: 165 passed;
  - frontend suite: 92 passed;
  - production frontend build: passed;
  - `git diff --check`: passed.

## DM campaign workspace and table workflow

- Objective: replace the remaining DM campaign placeholders with usable
  preparation and play tools before the practice sessions.
- Baseline commit: `8d2bd386eff7c40226958a0505a45701471b12ac`.
- Branch: `codex/first-edition-mapping`.
- Upstream state after `git fetch origin --prune`: zero ahead and zero behind.
- Pre-existing changes: none.
- Product decisions:
  - campaign Settings must provide a deliberate permanent-delete option in
    addition to Archive;
  - remove DM-side Journal and Treasure workspace tabs;
  - handouts are private uploads until the DM explicitly shares them, and only
    shared handouts are visible to campaign members;
  - NPCs are campaign-scoped name-and-notes records with add, edit, and delete;
  - session records have a session number/date, live notes, and categorized
    preparation items for NPCs, Secrets, Scenes, PC Notes, and Notes;
  - preparation items can be completed, edited, deleted, or forwarded into the
    next session's planning record;
  - Marching Order is the normal outside-combat table view; Start changes to
    Combat, round decrement never goes below one, and End clears the encounter
    and returns to Marching Order;
  - monster tokens use the same four within-square placement slots as player
    tokens.
- Planned data work: migration `0022_dm_campaign_workspace` for handouts, NPCs,
  session records, and session planning items. The implemented first handout
  slice stores authorized files in the local campaign database rather than a
  public static directory, so guessed frontend paths cannot expose them.
- Planned verification: campaign deletion safety, DM/player handout
  authorization, NPC/session CRUD and forwarding, migration chain, table-mode
  transitions, quadrant placement, backend/frontend suites, and production
  build.
- No website or VPS deployment is authorized.
- Implemented:
  - added migration `0022_dm_campaign_workspace` and campaign-scoped handout,
    NPC, session, and planning-item records;
  - handout uploads are limited to 15 MB PDF, image, or text files, remain DM
    only by default, and require both campaign membership and an explicit share
    flag before a player can list or open them;
  - DM handouts support upload, open, show/hide for players, and permanent
    delete; the player Handouts tab now lists and opens shared files;
  - NPC records support add, edit, save, and delete;
  - session records support add, edit, save, and delete, with separate running
    notes and NPCs, Secrets, Scenes, PC Notes, and Notes planning sections;
  - planning items support completion checkboxes, add, edit, delete, and
    idempotent forwarding into the next numbered session;
  - removed Journal and Treasure from the DM campaign workspace while
    preserving the private player Journal;
  - added permanent campaign deletion with exact-name confirmation; campaign
    workspace data is removed while owned characters are safely unassigned and
    returned to their players' vaults;
  - Marching Order is now the named, primary outside-combat table view;
  - compact Start, decrement, increment, and End combat controls enforce a
    minimum of round one, and End clears encounter state and returns to
    Marching Order;
  - standard monster tokens now use all four within-square token positions,
    matching player tokens.
- Verification:
  - fresh SQLite migration chain through `0022_dm_campaign_workspace`: passed;
  - complete backend/content suite: 168 tests passed;
  - frontend suite: 95 tests passed;
  - production frontend build: passed;
  - Python compilation and `git diff --check`: passed.
- Implementation commit:
  `0ec1903e` (`Build DM campaign preparation workspace`).
- Production deployment was not performed.

## Nightly closeout

- Tonight's completed work is recorded above: multi-class character support,
  Finder icon correction, campaign-authoritative Greyhawk/Dragonlance content,
  and the DM campaign workspace/table workflow.
- Final repository commits:
  - `0ec1903e` — `Build DM campaign preparation workspace`;
  - `8e208bf3` — `Record DM workspace release`.
- GitHub backup branch: `codex/first-edition-mapping`.
- Final upstream comparison before close: zero commits ahead and zero behind.
- Final worktree state before this closeout entry: clean.
- No website or VPS deployment was performed. Drago Table remains the
  locally operated application, with GitHub used only as the backup repository.
- Next use: restart Drago Table so migration `0022_dm_campaign_workspace` and
  the new frontend load, then exercise the DM and player workflows during the
  planned practice session.
