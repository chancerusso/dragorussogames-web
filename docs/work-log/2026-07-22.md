# 2026-07-22 Work Log

## Start-of-session review

- Objective: establish mandatory work and deployment logging before building
  Mapping Mode.
- Relevant notes reviewed:
  - `DRG1e_MasterInstructions.md` references and current project log locations
  - `docs/deployment.md`
  - `docs/architecture/CANONICAL_CONTENT_ARCHITECTURE.md`
  - `docs/changelog/2026-06-21.md`
  - `content/1e/source/CHANGELOG.md`
  - `content/1e/source/DECISIONS.md`
  - `content/1e/source/CONTENT_STATUS.md`
  - recent local and fetched remote Git history
  - ValveIO's work-log and deployment-record system as the requested model
- Branch at review: `codex/daggerheart-player-portal`
- Feature branch created before application code:
  `codex/first-edition-mapping`
- Baseline commit: `8f4f4d440da70f153acb6c4f5021ed5eddc44fc7`
- Upstream commit: `8f4f4d440da70f153acb6c4f5021ed5eddc44fc7`
- Upstream relationship: synchronized after `git fetch origin --prune`.
- Pre-existing worktree change: tracked deletion of
  `docs/sources/Dragonlance_Adventures_1e.pdf` from the source-library
  consolidation. All private 1e source PDFs now reside under the ignored
  `private-reference/sources/` library.
- Production commit: not verified.
- Deployment inventory status: repository facts verified; live VPS facts
  blocked by SSH timeout.

## Planned scope

- Add a repository working agreement that requires log review and updates.
- Add daily work-log templates and a current progress record.
- Add a separate deployment-record system.
- Strengthen the deployment runbook around verified targets and private source
  exclusion.
- Record the Mapping Mode product decisions without implementing feature code.
- Begin the first Mapping Mode implementation slice after the operating
  foundation is validated:
  - persistent campaign table mode and active map
  - persistent player map records and drawing state
  - DM-only mode, active-map, map creation, and Mapper assignment controls
  - Classic-only player map library, synchronized viewer, and Mapper editor
  - initial server-refresh synchronization before a later push channel

## Decisions

- Use the same operating-memory pattern as ValveIO: progress summary, daily
  work log, mandatory deployment runbook, live inventory, and per-deployment
  evidence.
- Do not begin Mapping Mode application code until this operating foundation is
  complete and validated.
- Do not deploy while the live server inventory is unverified.
- Treat the source-book consolidation deletion as pre-existing work and keep it
  distinct in the log.
- Mapping portal ownership was clarified after the initial documentation pass:
  the DM portal controls active mode and active map only. The Mapper editor,
  synchronized viewer, and player map library belong exclusively on the
  authenticated Classic player portal.
- Mapping state will be stored as structured JSON on campaign map records with a
  monotonically increasing revision. Player updates require both campaign
  membership and assignment as that map's Mapper.
- The first synchronization pass will use durable server saves and periodic
  viewer refresh. WebSocket or server-sent push is deliberately deferred until
  persistence, authorization, and the editing model are verified.

## Work completed

- Fetched and reviewed the current upstream repository state.
- Reviewed recent project history and the existing deployment notes.
- Confirmed the local deployment document matches the fetched remote version.
- Reviewed the neighboring project's established work-log and deployment
  conventions.
- Attempted read-only live VPS verification; SSH port 22 timed out.
- Added the permanent repository working agreement.
- Added the daily work-log system and Mapping Mode progress record.
- Added the deployment inventory and per-deployment record structure.
- Updated the deployment runbook with mandatory preflight and evidence rules.
- Corrected the Mapping Mode progress record to prevent player-facing mapping
  tools from being placed on `dm.dragorussogames.com`.
- Created branch `codex/first-edition-mapping` before application changes.
- Added Alembic revision `0011_campaign_mapping` with campaign table mode,
  active map, persistent map state, Mapper assignment, viewport, and immutable
  revision snapshots.
- Added DM-authenticated APIs for map creation, metadata updates, archive,
  active-map selection, and table-mode selection.
- Added player-authenticated map library and map reads with Mapper-only writes.
- Added the Classic player map library, active-map viewer, and pop-out Mapper
  desk.
- Added graph-paper drawing tools for snapped walls, freehand pencil, doors,
  secret-door suspicions, windows, stairs, traps, pits, numbered notes, color,
  erasing, undo, and redo.
- Added debounced autosave, revision creation, saved viewport, 2.5-second
  background viewer refresh, and silent refreshes that preserve the active tool
  and undo history.
- Replaced the initial prompt-based note entry with direct circled-marker
  placement followed by typing in the notes ledger.
- Added local `classic.localhost` and `dm.localhost` hostname support for portal
  testing without weakening production host separation.
- Refined Mapper interaction after the first production review: notes, pits,
  traps, and stairs now snap to square centers; doors, secret doors, and windows
  snap to the nearest wall-line midpoint; right-click ends a connected wall
  chain; Move now drags placed objects; note text is smaller; and ink colors are
  stacked vertically below the tools.
- Follow-up refinement normalizes previously saved symbols as well as new ones,
  places the vertical ink column below Undo/Redo, disables accidental SVG text
  selection while drawing, hardens object dragging, uses key-sized regular note
  typography, and lets the assigned Mapper rename the map.
- Removed the redundant oversized Exploration Flow heading and constrained all
  expanded exploration-rule text to wrap within the player-table side panel.
- Replaced the player Journal placeholder with a private, autosaving campaign
  journal for each campaign member. Journal text is stored on the membership,
  so one player's notes are not exposed to another player.
- Changed player-facing character, First Edition rules, and campaign-reference
  links that leave the campaign table to open in a separate browser tab. The
  active table remains open and retains its current state.

## Verification

- [x] Documentation link and consistency check
- [x] `git diff --check`
- [x] Git diff review
- [x] Confirmed the operating-foundation phase changed no Mapping Mode
  application code before implementation began
- [x] Confirmed private source PDFs remain ignored and absent from frontend
  source and built output
- [x] Fresh SQLite migration through `0011_campaign_mapping`
- [x] Mapping API authorization test: DM control, campaign-member read,
  assigned-Mapper write, non-Mapper write denial, and revision persistence
- [x] Frontend tests: 65 passed
- [x] Frontend production build
- [x] Frontend tests after Mapper refinement: 68 passed
- [x] Frontend tests after production-feedback follow-up: 69 passed
- [x] Frontend tests after player-journal and new-tab changes: 69 passed
- [x] Fresh SQLite migration through `0014_player_campaign_journal`
- [x] Player journal API test: separate campaign members retain separate notes
- [x] Frontend production build after player-journal changes
- [x] Mapper-only map rename authorization test
- [x] Browser: DM portal renders mode/map/Mapper controls without player canvas
- [x] Browser: Classic portal renders active map and saved map library
- [x] Browser: Mapper drew walls, placed a numbered note, edited its ledger text,
  autosaved, reloaded, and retained all state
- [x] Browser: no new Mapping Mode console errors after replacing prompt entry;
  existing React Router v7 future warnings remain
- [x] Public predeployment check: API health, DM portal, and Classic portal
  respond successfully. Private source-path probes return the application HTML
  fallback rather than PDF content.
- [ ] Aggregate Python content suite: blocked by the pre-existing canonical
  validation error `unknown type 'magic_item_catalog'` in
  `content/osric/core/magic_items/index.json`. The new mapping test passes alone.

## Repository activity

- Feature commit: `af4d2d5` (`Build persistent first-edition mapping mode`)
- Push: `codex/first-edition-mapping` published to `origin`
- Pull requests: none

## Deployment activity

- Deployment performed: Yes, application commit `78da89e` on 2026-07-23 UTC.
- Deployment record: `docs/deployments/2026-07-23-mapping-mode.md`
- Aborted predeployment attempt: backup succeeded, but migration correctly
  stopped because the first Mapping candidate did not contain production's
  `0012_campaign_table_state`. No migration or frontend copy occurred.
- Production was restored to `codex-osric-monster-catalog` commit `1100e11`;
  backend service and health check passed.
- Correct VPS workflow recovered: connect to
  `valves-nyc2.stonecat-marlin.ts.net`, elevate with `sudo -i`, and deploy from
  the root-owned `/opt/russo-bot/source` checkout.
- Corrected Mapping candidate was built successfully and copied separately to
  the Classic and DM web roots. Public backend health and both portal roots
  returned successful responses.

## Blockers and risks

- Live server identity, production branch, deployed commit, service, database
  migration, and management route are now verified and recorded.
- Correction: `dm.dragorussogames.com` is a portal hostname, not an assumed VPS
  management address. Player-facing Mapping Mode deploys to the Classic web
  root; only DM controls deploy to the DM web root. Future deployment records
  must treat backend, DM frontend, and Classic frontend as explicit targets.
- The current branch name reflects earlier Daggerheart work and should be
  replaced for Mapping Mode. Completed with `codex/first-edition-mapping`.
- Current viewer synchronization is durable background refresh, not WebSocket
  push. Concurrent-edit conflict handling is still needed before more than one
  writer is ever permitted.
- The aggregate Python content suite has an existing canonical schema failure
  unrelated to this feature.
- Mapping has now been reconciled with production and sequenced as migration
  `0013_campaign_mapping` after production migration `0012_campaign_table_state`.
- Player journals add migration `0014_player_campaign_journal`; deployment must
  back up PostgreSQL and run Alembic before restarting the backend.

## Next starting point

- Review the first Mapper experience with the user.
- Add editable map names, floors/levels, zoom, better pan behavior, and revision
  restoration.
- Decide whether to add WebSocket or server-sent push synchronization after the
  interaction model is accepted.
- Do not deploy until the live inventory blocker is resolved.

## Mapper and combat correction session

- Objective: reconcile the visibly stale production Mapper with the committed
  Mapper fixes, then complete the player combat-flow, weapon-speed, and live
  session presentation changes as one locally verified release.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline local commit: `16e40e9ea1e3194f8e44959152f85a726b499e43`.
- Pre-existing worktree changes: none.
- Production evidence: the latest recorded deployment is still application
  commit `78da89e426d87e9d2d957734dde2b6f24b4b6781`; its Mapper source contains
  the old horizontal colors and old movement behavior.
- Deployment defect: the previous assistant response supplied an incorrect
  full SHA. With `set -e`, its verification command would stop deployment
  before build or copy. No later deployment is considered proven until its
  served asset names and visible Mapper behavior are recorded.
- Relevant references reviewed: working agreement, progress log, current work
  log, deployment standard, deployment inventory, latest deployment record,
  Drago Table game plan, Classic stabilization plan, and the purchased First
  Edition Player's Handbook combat and combined-weapons-table pages.
- Planned verification: catalog/runtime tests, frontend tests, production
  build, private-source exclusion, and authenticated local browser inspection
  of Mapper, combat rules, weapon speed, and live-session status.
- Completed the corrected player-table release locally:
  - Surprise is step one; the combat panel explains when First Edition weapon
    Speed Factor matters and that lower numbers are quicker.
  - Weapon Speed Factor is carried from the catalog through equipped-weapon
    runtime data and displayed in player summaries, character sheets,
    proficiency rows, inventory, campaign equipment, and DM equipment editing.
  - Live Session is a filled green status.
  - Player token color saves are protected from both in-flight polling and a
    stale poll that began immediately before the save.
  - The previously committed Mapper corrections remain present: square-center
    symbols, smaller note text, vertical colors below Undo/Redo, movable map
    objects, right-click wall reset, and Mapper-controlled map renaming.
- Verification completed:
  - Focused backend/content tests: 46 passed.
  - Frontend tests: 73 passed, including the final stale-poll regression.
  - Production build: passed; candidate assets are
    `index-D9dKcO98.js` and `index-6iuCmQ6K.css`.
  - Fresh SQLite migration chain reaches `0015_weapon_speed_factors`.
  - Authenticated browser: Live Session renders green, Surprise is first,
    Sword, long renders `Spd 5`, and a newly selected blue token remains On Grid
    after multiple background-refresh intervals.
- Deployment status: pending. Nothing from this correction session is recorded
  as deployed until the exact pushed SHA, migrations, Classic assets, health,
  and public smoke checks are captured.

## Player's Handbook equipment audit

- Objective: replace the remaining OSRIC-derived equipment data with
  Player's Handbook-authoritative structured values, beginning with the full
  weapon and missile tables.
- Required weapon fields: exact printed S/M damage, L damage, Speed Factor,
  fire rate, and separate Short/Medium/Long range values in inches. Do not
  convert those stored range values to feet or yards and do not label them
  "Range Increment."
- Baseline branch: `codex/first-edition-mapping`.
- Baseline commit: `85f373c7b3be8372d0d98fad9fdfa276572b356f`.
- Upstream: local and `origin/codex/first-edition-mapping` are equal after
  `git fetch origin --prune`.
- Pre-existing worktree changes: none.
- Source reviewed: purchased First Edition Player's Handbook PDF, especially
  printed pages 37-39 and the reference tables on printed pages 123-124.
  Private source pages remain outside the repository and all builds.
- Initial findings:
  - Speed Factor is a weapon property and does not vary with opponent size.
  - Weapon damage has distinct S/M and L values.
  - Missile ranges are explicit Short, Medium, and Long values in inches;
    indoor/outdoor conversion is a separate play-time scale.
  - The current catalog is an incomplete OSRIC-derived subset, uses converted
    feet under "Range Increment," and must not be described as PHB-complete.
  - The PHB main and condensed reference tables contain at least one apparent
    conflict, so discrepancies require an explicit audit record rather than
    silent OCR import.
- Deployment status: paused until the PHB equipment correction is complete,
  tested, committed, and assigned a new exact deployment SHA.
- Planned verification: table-by-table source comparison, seed/parser tests,
  migration of existing catalog rows, runtime/UI checks, frontend tests,
  production build, migration chain, and private-source exclusion.
- Follow-on audit scope: PHB spell lists, spell records, player procedures, and
  general rules must receive the same book/page/verification treatment. DMG
  procedures and clarifications supersede temporary OSRIC text where the DMG
  is the controlling source. Monster Manual work will preserve complete
  structured statistics and all prose necessary to operate special abilities,
  while omitting non-mechanical descriptive filler.

## Player's Handbook spell audit

- Objective: replace the OSRIC-labelled player spell index and runtime
  provenance with a Player's Handbook-authoritative spell-list checkpoint,
  while leaving individual spell mechanics explicitly pending until each
  printed entry is verified.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline commit: `88ba65debbbe48fc56730f33eb0a0d1266407112`.
- Upstream: local and `origin/codex/first-edition-mapping` are equal after
  `git fetch origin --prune`.
- Pre-existing worktree changes: none.
- Source: purchased First Edition Player's Handbook PDF. The private scan will
  be read and visually checked outside the repository and must not enter any
  commit, frontend build, API response, or deployment.
- Intended scope:
  - verify the four PHB spell lists (cleric, druid, magic-user, illusionist);
  - correct names, class/list membership, and spell levels in the canonical
    player index;
  - attach PHB book/page and verification metadata to runtime spell records;
  - remove OSRIC authority language from the spell landing/index pages;
  - preserve individual spell text as pending unless its full PHB entry has
    been audited.
- Planned verification: source-list comparison, focused backend/content tests,
  frontend tests, fresh migration chain, production build, private-source
  exclusion, exact pushed commit comparison, and the mandatory production
  deployment preflight.
- Completed the PHB spell-list checkpoint:
  - visually verified the cleric, druid, magic-user, and illusionist tables on
    printed pages 40-42;
  - recorded 416 class/level entries representing 350 distinct spells;
  - corrected PHB names including `Detect Snares & Pits`, `Chariot Of
    Sustarre`, `Spiritual Hammer`, and the separate `Project Image` /
    `Projected Image` records;
  - added class-specific spell levels so shared spells no longer inherit only
    their lowest level (for example, Cure Light Wounds is Cleric 1 and Druid
    2);
  - replaced the player spell index and all four class-list pages with the PHB
    tables and Player's Handbook provenance;
  - added migration `0017_phb_spell_lists` to preserve referenced spell IDs
    while renaming legacy rows, inserting missing rows, and adding source,
    printed-page, verification, and class-level metadata.
- Individual spell range, duration, components, area, casting time, saving
  throw, and operating text remain explicitly pending entry-by-entry PHB
  verification. Existing unaudited mechanics are not relabelled as verified.
- Verification completed:
  - focused backend/runtime tests: 48 passed;
  - frontend tests: 73 passed;
  - frontend production build passed with `index-BlCX0SRO.js` and
    `index-6iuCmQ6K.css`;
  - fresh SQLite migration chain reached `0017_phb_spell_lists` and contained
    350 spell rows;
  - generated player spell links all resolve to a spell page;
  - aggregate content suite: 145 passed, 1 pre-existing failure in the
    Dragonlance private-PDF coverage report because its local source scan is
    unavailable to that test.
- Source-policy decision: official books are the trusted sources. Remove OSRIC
  from user-facing labels and provenance section by section as each official
  source audit completes. Legacy identifiers and compatibility columns may
  remain temporarily during migration, but must not be presented as authority.
- Completed first PHB weapon slice:
  - Replaced the partial master table with 53 visually verified PHB
    weapon/ammunition purchase rows and complete length, space, Speed Factor,
    S/M damage, L damage, gold-piece weight, and cost fields.
  - Replaced converted "Range Increment" values with 15 PHB missile-use
    profiles containing exact Short/Medium/Long inch values and fire rates.
  - Runtime and player/DM displays preserve and show `S / M / L` ranges.
  - PHB gold-piece weapon weight is retained verbatim in source properties and
    converted at 10 gp per pound only for the existing encumbrance runtime.
  - PHB ammunition purchase bundles now distinguish singles, dozens, and
    scores without losing physical quantity, weight, or value calculations.
  - Main player navigation now says `Player's Guide`; equipment pages and
    catalog links identify the Player's Handbook rather than OSRIC.
  - General gear and armour remain explicitly marked pending PHB audit.
- Migration: added `0016_phb_equipment_catalog`, which synchronizes verified
  PHB rows, inserts missing official choices, and archives superseded
  OSRIC-only choices without deleting inventory references.
- Verification:
  - Focused character/runtime backend tests: 45 passed.
  - Frontend tests: 73 passed.
  - Fresh SQLite migration chain: `0016_phb_equipment_catalog (head)`.
  - Frontend production build passed with `index-BlCX0SRO.js` and
    `index-6iuCmQ6K.css`.
  - `git diff --check` and private-source build scan passed.
- Next audit slice: finish PHB general equipment, armour, shields, costs, and
  source gaps; then begin the spell index and per-spell PHB verification.

## PHB general equipment and armour checkpoint

- Objective: replace the remaining OSRIC-derived money, starting funds,
  armour, shields, clothing, herbs, livestock, general gear, provisions,
  religious items, tack, and transport with visually verified PHB values.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline commit: `e4778f647e79863dd4816ac501e753844bdfd803`.
- Upstream: local and `origin/codex/first-edition-mapping` are equal after
  `git fetch origin --prune`.
- Pre-existing worktree changes: none.
- Trusted source: purchased First Edition Player's Handbook, printed pages
  35-38 and the condensed reference tables on printed pages 123-124.
- Source rule: where the PHB supplies cost but no carried weight, preserve the
  exact cost and explicitly mark weight as not supplied. Do not retain an
  OSRIC weight while labeling the row as PHB-verified.
- Planned verification: visual source comparison, catalog/parser tests,
  migration chain, frontend tests and build, private-source exclusion, and
  explicit review of any main-table/reference-sheet conflicts.
- Completed PHB general equipment and armour checkpoint:
  - Replaced OSRIC shop rows with the complete PHB cost-list categories:
    clothing, herbs, livestock, miscellaneous equipment, provisions, religious
    items, tack and harness, and transport.
  - Replaced armour choices and names with the PHB list, including both helmet
    entries and the separate wooden small shield.
  - Stored exact PHB costs, armour classes, category, book, printed page, and
    verification status for every row.
  - Retained runtime weights and armour movement caps only where the existing
    character engine requires them, and marked both explicitly as pending
    official-book verification rather than PHB-derived.
  - Resolved the PHB plate-barding conflict in favor of the condensed reference
    value `500 gp`; the main cost page visibly prints the implausible `15 sp`.
- Catalog result: 173 active PHB-aligned equipment records after name
  deduplication, including the previously verified weapon and missile records.
- Verification:
  - Focused backend character/runtime tests: 46 passed.
  - Frontend tests: 73 passed.
  - Fresh SQLite migration chain: `0016_phb_equipment_catalog (head)`.
  - Production frontend build passed with `index-BlCX0SRO.js` and
    `index-6iuCmQ6K.css`.
  - `git diff --check` and private-source build scan passed.
- Next checkpoint: PHB spell lists and spell records, followed by the broader
  player-rules audit. Official DMG rules will resolve remaining runtime-weight,
  movement, combat, and adjudication fields.
