# Work Log — 2026-07-24

## Start-of-session review

- Objective: resume from the accepted native Drago Table local experience,
  review yesterday's completed work, and begin the next focused refinement
  without changing the VPS deployment.
- Relevant notes reviewed:
  - `docs/work-log/PROGRESS.md`;
  - `docs/work-log/2026-07-23.md`;
  - `docs/architecture/LOCAL_DRAGO_TABLE_HOST.md`;
  - `docs/local-host.md`.
- Branch: `codex/first-edition-mapping`.
- Baseline commit:
  `dae15e8644c2e783407d2ba1aeef0935fb28dd8e`.
- Upstream commit:
  `dae15e8644c2e783407d2ba1aeef0935fb28dd8e`.
- Upstream relationship after `git fetch origin --prune`: local HEAD exactly
  matches `origin/codex/first-edition-mapping` (zero ahead, zero behind).
- Pre-existing worktree changes: none.
- Production commit:
  `9c12ea3ca4ada443bceae921a5bd1d1cd7597d85`, as recorded in
  `PROGRESS.md`; the native local-host work is not a VPS deployment.
- Deployment inventory status: not reread because no deployment is planned.
  The deployment runbook, inventory, latest deployment record, exact pushed
  commit, backups, migrations, health checks, and smoke tests must all be
  reviewed before any later deployment.

## Planned scope

- Preserve yesterday's accepted native launcher and loopback-only local
  runtime.
- Add a dedicated Drago Table Cloudflare hostname and tunnel without changing
  or interrupting the existing Foundry tunnel.
- Add one-click remote-session lifecycle and player-invite controls to the
  native launcher.
- Verify authenticated DM, Mapper, and ordinary viewer behavior through the
  public hostname.
- Refine the player sign-in, Mapper workspace, and launcher presentation.
- Keep LAN mode, QR presentation, and VPS deployment outside the current item.
- Record decisions, files, checks, defects, commits, pushes, and next actions
  as work proceeds.

## Decisions

- Start from the clean, fully synchronized branch rather than reopening or
  amending yesterday's completed commit.
- Treat the local application and production VPS as separate release states.
- Reuse the proven Foundry access model for Drago Table: a named Cloudflare
  Tunnel running only on the host Mac, a normal HTTPS player URL, and no VPN,
  Tailscale, WARP, or other networking client on player devices.
- Keep the browser as the primary player experience. If offline rules and
  character creation are added, prefer an optional installable Progressive Web
  App over separately packaged Mac and Windows player executables.
- Keep live campaign state authoritative on the host. Offline player data will
  need an explicit synchronization and conflict policy before implementation.
- Use a separate `drago-table` tunnel rather than adding Drago Table to the
  existing Foundry tunnel. The launcher therefore owns only Drago Table's
  remote-session lifecycle.
- Use `table.dragorussogames.com` as the public session hostname and
  `https://table.dragorussogames.com/portal` as the player invite.
- Retain application-level player authentication as the primary access gate;
  do not add a second Cloudflare Access login to the initial player journey.

## Work completed

- Reviewed the prior session and current project progress.
- Refreshed remote references and confirmed a clean synchronized baseline.
- Audited the existing local Foundry tunnel configuration without reading or
  recording its credentials:
  - the named tunnel publishes `foundry.dragorussogames.com`;
  - it proxies to Foundry at `http://localhost:30000`;
  - unmatched ingress receives HTTP 404;
  - the installed Apple Silicon `cloudflared` version is `2026.3.0`.
- Confirmed from current Cloudflare documentation that published HTTP
  applications use outbound-only host connections and require no player-side
  Cloudflare client.
- Audited the current frontend: it has same-origin API support and limited
  local browser storage, but no web-app manifest, service worker, offline cache,
  or offline/server character synchronization layer yet.
- Created the dedicated Cloudflare Tunnel `drago-table` and corrected its DNS
  route to the new tunnel after the locally managed CLI initially inherited
  the Foundry tunnel from the default configuration.
- Added `table.dragorussogames.com` as the dedicated public hostname. The
  existing `foundry.dragorussogames.com` route remains unchanged.
- Redesigned the native Mac launcher with separate local and remote status
  cards plus:
  - **Start Remote Session**;
  - **Stop Remote Session**;
  - **Copy Player Invite**;
  - verified public-health state before reporting the session live.
- Added a bundle-owned, non-secret remote-session descriptor. Tunnel
  credentials remain outside the repository under the Mac user's private
  `.cloudflared` directory.
- Added a private Application Support tunnel configuration and separate tunnel
  log. Quitting Drago Table stops the tunnel before stopping the local host and
  private database.
- Made the public Drago hostname part of the unified local/player routing
  model. Remote players no longer see a misleading **Return to DM Portal**
  action, and the DM **Player View** link remains on the same host.
- Refined the remote player sign-in with a centered layout, live-session
  indicator, direct account guidance, and **Join the Table** action.
- Recolored the Mapper workspace from the older brown/gold treatment to the
  cabernet, dragon red, blue, cool charcoal, and ivory Drago Table palette.
- Installed the verified native launcher at
  `/Users/ChanceRusso/Desktop/Drago Table.app`. The previous launcher was moved
  recoverably to the Trash.
- Remote interaction verification:
  - launcher started the local service and reported it ready;
  - launcher started the dedicated tunnel and verified the public endpoint;
  - public Player login rejected invalid credentials;
  - public DM authentication returned the local test campaign;
  - an authenticated temporary Mapper saved a freehand update as revision 30;
  - a different authenticated viewer received revision 30, including the new
    stroke, with `can_edit: false`;
  - browser reload reconnected the Mapper without losing authentication;
  - the map was restored to its pre-test revision and prior Mapper assignment;
  - both temporary accounts were removed from the campaign and deactivated;
  - stopping only the remote session left local health at HTTP 200 and changed
    public health to unavailable;
  - stopping Drago Table closed both ports 8010 and 55432.

## Verification

- [x] Relevant automated tests: 84 frontend and 154 backend/content tests pass.
- [x] Production build: Vite build passed.
- [x] Focused browser or API smoke test: installed launcher, public DM, Mapper,
  viewer, reconnect, invite, tunnel stop, and clean application shutdown pass.
- [x] Private-reference exclusion check: production assets contain no private
  source path or private rulebook filename.

## Repository activity

- Commits:
  - `9f9f78f` — Add one-click Drago Table remote sessions.
  - `16e6fba` — Require fresh login and fix remote status light.
- Pushes: implementation and session record pushed to
  `origin/codex/first-edition-mapping`.
- Pull requests: none.

## Deployment activity

- Deployment performed: No.
- Deployment record: none.

## Blockers and risks

- An offline character builder introduces versioning, source-data caching,
  local data protection, server reconciliation, and conflict-resolution work.
- Distribution of offline rules content must stay within the project's
  applicable content licenses; private official-book sources must never be
  included.
- The installed `cloudflared` version is `2026.3.0`; Cloudflare reports
  `2026.7.3` as current. Upgrade should be scheduled outside a live session and
  followed by a tunnel smoke test.
- LAN access is not yet implemented; the current runtime intentionally binds
  only to `127.0.0.1`. Remote access does not require LAN binding because
  `cloudflared` runs on the same host.

## Next starting point

- Run a player group acceptance session using the installed launcher and one
  shared invite. Next refinements: QR presentation, clearer connection-error
  and reconnect states, and the optional installable/offline Player PWA as a
  separate project.

## DM session-expiry correction

- First user acceptance test exposed an expired-session defect while creating
  a campaign: the backend correctly returned HTTP 401, but the React
  authentication provider retained its old in-memory state. The protected DM
  shell therefore stayed visible and displayed `Authentication required`
  instead of returning to sign-in.
- Updated the shared authentication token setters to emit a role-specific
  authentication-change event whenever an admin or player token is added or
  removed.
- Updated the authentication provider to follow those events and immediately
  refresh the affected role state. Any API 401 now clears the token and causes
  the protected route to redirect to the appropriate DM or Player login.
- Added a regression test covering both admin and player session-expiry
  notification wiring.
- User clarified the required security behavior: every Drago Table application
  startup must begin with a fresh login. The local runtime now creates a new
  session-signing key on every launch, invalidating all DM/player tokens and
  cookies from the previous run while preserving accounts and campaign data.
- React authentication tokens now use browser session storage and remove
  legacy persistent local-storage tokens. Added regression coverage for both
  the runtime key rotation and admin/player session-only token storage.
- Restart verification passed against the installed Desktop application:
  stopped the running local and remote session, reopened Drago Table, navigated
  directly to `/campaigns`, and confirmed that only the Dungeon Master password
  screen is available. Remote access remains off until explicitly started.
- User acceptance then exposed that the native launcher reported the public
  session live while its Remote players indicator remained gray. The indicator
  had been created with a fixed initial color. It now follows tunnel state:
  amber while connecting, green after the public health check, gray when
  deliberately stopped, and red after a remote failure or unexpected stop.

## Player character-builder authentication correction

- Player acceptance exposed that **Create Character** could leave the React
  player portal for the classic static builder, where an unauthenticated
  handoff incorrectly targeted the Dungeon Master login.
- Scope: keep player navigation inside player-owned routes, direct expired
  character-builder sessions to Player Login, and make the classic builder
  read the same session-only player token as the React portal.
- Planned verification: focused routing/token regression tests, full frontend
  tests and build, backend tests, and an installed-app player-flow smoke test.
- Corrected the Player header so **Create Character** stays within the
  player-owned React routes before handing off to the campaign-aware builder.
- Updated the classic character builder to use session storage for its player
  bearer token, matching the portal's fresh-login-on-startup policy.
- Updated unauthenticated First Edition player routes to use Player Login on
  the unified Drago Table hostname. Classic continues to use its own `/login`;
  `/1e/dm` continues to use Dungeon Master Login.
- Verification passed:
  - 85 frontend tests;
  - Vite production build;
  - 157 backend/content tests, including three new redirect tests;
  - installed-app smoke test with a disposable player: Player Login →
    **Create Character** → **Build a Character**, retaining the player identity
    and campaign without showing Dungeon Master Login.
- The disposable player was removed from the campaign and deactivated after
  verification. The app was restarted with remote access deliberately left
  off.

## Player sign-in heading refinement

- Reduced the Player Sign In heading size and prevented it from wrapping at
  the narrow player-login window width requested during acceptance.
- Planned verification: frontend tests, production build, and narrow-window
  visual check.
- Verification: all 85 frontend tests and the Vite production build pass. The
  scoped heading rule caps the player title at 32px and keeps it on one line.
  A 520×630 local render confirms the complete heading fits cleanly inside the
  narrow login card.

## Finder startup PostgreSQL discovery correction

- A subsequent Finder launch failed before health check because the native app
  inherited a minimal macOS GUI `PATH` and could not resolve `postgres`, even
  though Homebrew PostgreSQL remains installed at `/opt/homebrew/bin`.
- Scope: make the local launcher discover required Homebrew commands from
  stable Apple Silicon and Intel locations without depending on Terminal shell
  initialization, then smoke-test the native app.
- The launcher now checks `/opt/homebrew/bin`, `/usr/local/bin`, and versioned
  Homebrew PostgreSQL `opt` links after normal `PATH` resolution.
- Verification passed:
  - a deliberately minimal Finder-style `PATH` resolves `postgres`, `pg_ctl`,
    and `psql` to the installed Apple Silicon Homebrew commands;
  - all 86 frontend tests pass;
  - a real desktop-app stop and relaunch reaches green **Ready — available on
    this Mac** and enables the DM, Player View, and Remote Session controls.
- Existing database content was preserved. Remote access was left off.
- A later user-initiated cold start exposed a second, distinct Finder
  environment defect. PostgreSQL resolved successfully but aborted with
  `postmaster became multithreaded during startup` and instructed the launcher
  to provide a valid `LC_ALL`.
- The launcher now pins its private host subprocesses to the neutral `C`
  locale before invoking PostgreSQL. Planned verification was expanded to
  repeated desktop cold starts to catch the intermittent Finder-only failure.
- Verification passed under an environment stripped to Finder-like essentials:
  the launcher restores `LC_ALL=C` and `LANG=C` and resolves Homebrew
  PostgreSQL. All 86 frontend tests pass.
- Three consecutive native desktop stop-and-start cycles then completed:
  PostgreSQL reported `database system is ready to accept connections`, the API
  health check returned HTTP 200, and the launcher reached green **Ready —
  available on this Mac** every time. The third verified instance remains
  running locally with remote access off.

## Remaining player character-route ownership correction

- User acceptance found another path to Dungeon Master Login: a campaign
  overview used a React `Link` for the classic `/1e/characters/new/` document.
  On the unified local/table hostname React matched that URL against the
  DM-owned route group before the browser requested the player-authenticated
  static builder.
- Multi-campaign character selection also used `/characters/new`, which is a DM
  route on the unified hostname. Player character View and Edit links had the
  same ambiguous unprefixed route shape.
- Corrected all four player paths: the chooser now uses `/portal/characters/new`
  on unified hosts, while builder/view/edit handoffs use ordinary browser links
  to the player-authenticated `/1e/characters/...` documents.
- User then clarified that every click from the already-open player page
  requested authentication. The repeated native startup verification had
  intentionally rotated the session-signing secret three times, so that page
  was visibly open with an expired player session. This explains the repeated
  authentication request but not the incorrect DM destination.
- Completed the wider player-link audit:
  - the static character vault now recognizes the current player session;
  - player character View and Edit URLs carry explicit player context;
  - character-builder Home, Campaign, Characters, and Dragonlance links use
    `/portal/...` on the unified local and public table hosts;
  - Classic-host paths remain unprefixed.
- Verification passed with one uninterrupted disposable-player login and no
  application restart: Campaigns, Characters, Create Character, Player's
  Guide, Character Vault, and Back to Player Home all opened without another
  login and never showed Dungeon Master Login. The disposable account was
  removed from the campaign and deactivated afterward.
- All 86 frontend tests and the production build pass. Drago Table remains
  running locally; this final verification startup intentionally invalidated
  any browser session from before it, so the user needs one fresh login now.

## Cheili character ownership correction

- Objective: correct a character created under the wrong duplicate player
  record and prevent ambiguous player selectors from allowing the same mistake.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline and upstream commit after `git fetch origin --prune`:
  `ddcfefd5406ed5f6c98b227ee0bfa065e854aa47`.
- Pre-existing changes: none.
- Diagnosis:
  - two active player records both display as `Cheili`;
  - character `Cheiliveth Victorino` belongs to legacy username `test`;
  - the player logs in as username `cheili`, which currently owns no
    characters;
  - both records belong to campaign `Live Testing`.
- Planned scope:
  - transfer only `Cheiliveth Victorino` to the authenticated `cheili` record;
  - preserve the unrelated `Just a Guy` test character and `Solo Test`;
  - label administrative player choices with display name, username, and
    stable ID;
  - add regression coverage and verify the player character journey.
- No deployment is authorized or planned in this correction.
- Data correction completed:
  - moved only character ID `2`, `Cheiliveth Victorino`, from legacy player ID
    `1` (`test`) to player ID `4` (`cheili`);
  - preserved campaign ID `2`, `Live Testing`, and active status;
  - preserved `Just a Guy` under the legacy `test` account and `Solo Test`;
  - verified both records now own one active character and no unrelated record
    changed.
- Application correction completed:
  - shared select rendering now supports explicit option values and labels;
  - administrative owner, current-player, and character-filter selectors show
    display name, username, and stable player ID.
- Verification completed:
  - 87 frontend tests pass, including the duplicate-player-name regression;
  - the Vite production build passes;
  - the complete backend/content unittest discovery command passes;
  - local and tunneled character-builder routes return Player Login, not
    Dungeon Master Login, when unauthenticated;
  - the active public Drago Table session serves the corrected character-vault
    asset.
- Deployment activity: no VPS deployment. The correction is active in the
  locally hosted Drago Table build and its current remote tunnel session.
- Repository activity:
  - commit `1feafd3` — `Disambiguate character ownership`;
  - pushed to `origin/codex/first-edition-mapping`.

## Player builder and table usability pass

- Objective: apply the player-facing fixes from the supplied builder, ability
  roll, DM table, and player table screenshots.
- Baseline branch and commit: `codex/first-edition-mapping` at
  `2bf727c029b0437975bcc0b2a04efc64644eaf5d`.
- Upstream state: synchronized with
  `origin/codex/first-edition-mapping` after fetch.
- Pre-existing changes: none.
- Planned scope:
  - simplify the builder identity and campaign fields;
  - make rolled ability values single-use and reversible;
  - require exceptional Strength for a natural 18 where applicable;
  - restore the out-of-combat Marching Order;
  - add player combat status, dice rolling, and shared roll history.
- No VPS deployment is authorized or planned.
- Implementation completed:
  - New Player supports one or more campaign invitations at account creation;
  - campaign rosters use explicit Invite language and disambiguate duplicate
    display names with usernames;
  - the player builder lists only invited campaigns and permits Pending;
  - every player character card can attach, move, or unassign that character
    among invited campaigns, with one owned character per campaign;
  - DM player deletion permanently removes the account, owned characters, and
    invitations after a count-specific confirmation;
  - DM and player character deletion permanently remove the selected character
    after an irreversible-action confirmation;
  - backend membership checks prevent a player from assigning a character to
    any campaign outside their invitations.
- Verification completed:
  - all 90 frontend tests pass;
  - all 159 backend/content tests pass, including the new invitation,
    unauthorized-assignment, character-deletion, and player-cascade cases;
  - the Vite production build passes;
  - `git diff --check` passes.
- Implementation completed:
  - the player builder shows the authenticated player once, removes the
    redundant player-name field and explanatory subtitle, and omits Campaign
    Day and Current Location;
  - rolled ability values disappear from other selectors, selected roll chips
    change color, and clicking a used chip returns that ability to Manual;
  - natural Strength 18 exposes and requires the exceptional d100 entry for
    eligible warrior classes;
  - exploration mode again displays a 2-by-6 Marching Order while outdoor
    travel retains the hex grid;
  - the player-side Combat Flow now includes combat status, a dice roller, and
    a shared history identifying the rolling character or Dungeon Master.
- Verification completed:
  - all 89 frontend tests pass, including regressions for player identity,
    reversible single-use ability rolls, Marching Order, and shared dice
    history;
  - the Vite production build passes;
  - `git diff --check` passes.
- A live browser review was not performed because the local Drago Table service
  was not running. It was deliberately not restarted because startup correctly
  invalidates existing sessions, and preserving the user's next login was more
  important than an unnecessary restart.
- Repository activity:
  - commit `e7fe0e7` — `Improve player builder and table tools`;
  - pushed to `origin/codex/first-edition-mapping`.

## Invitation-owned campaign and roster management

- Objective: let the DM permanently delete obsolete players and characters,
  make newly created players immediately inviteable, and let each player attach
  their own characters only to campaigns to which they were invited.
- Baseline branch and upstream commit after fetch:
  `codex/first-edition-mapping` at
  `e6713286c13b223bd83807468d020a60533120a6`.
- Pre-existing changes: none.
- Intended scope:
  - guarded permanent-delete actions for DM player and character rosters;
  - optional campaign invitations while creating a player;
  - campaign selection in the player builder and player character roster;
  - server-side membership enforcement for every player campaign assignment;
  - regression coverage for deletion and invitation boundaries.
- No VPS deployment is authorized or planned.
