# Work Log — 2026-07-23

## Local Drago Table Host — Phase 1

- Objective: begin building Drago Table as a locally operated browser
  application that can be prepared and tested without an internet connection
  or VPS deployment.
- Intended scope for the first implementation slice:
  - preserve the existing React, FastAPI, database, authentication, campaign,
    Mapping Mode, Combat Mode, and rules foundations;
  - add an explicit loopback-only local runtime profile;
  - serve the built application and API from one local origin;
  - provide one documented command to migrate, start, health-check, open, and
    cleanly stop the local application;
  - keep the existing VPS production topology available and unchanged.
- Architecture decision:
  `docs/architecture/LOCAL_DRAGO_TABLE_HOST.md`.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline commit:
  `ec9fd7afc59d9bca7f78eabafea93d635a6c7ec1`.
- Upstream state after `git fetch origin --prune`: local HEAD exactly matches
  `origin/codex/first-edition-mapping`.
- Pre-existing changes: the user-supplied, untracked
  `docs/architecture/LOCAL_DRAGO_TABLE_HOST.md`; it defines this work and will
  be preserved rather than treated as unrelated generated output.
- Initial configuration audit:
  - the frontend API base already defaults to same-origin `/api`;
  - portal selection still depends on production hostnames and `.localhost`
    aliases;
  - several interface actions still link directly to the Classic production
    hostname;
  - backend CORS defaults list the public production origins;
  - FastAPI can already mount a built static site, but the current site-root
    and lifecycle need a supported local production wrapper;
  - authentication cookies are path-scoped but need profile-driven secure and
    same-site behavior;
  - PostgreSQL remains the initial local database choice pending an explicit
    SQLite qualification effort.
- Planned verification:
  - run the complete frontend and backend tests;
  - produce the frontend build and serve it through the local FastAPI origin;
  - migrate a disposable local database through the complete Alembic chain;
  - verify health, DM route, player route, and authentication behavior on
    loopback;
  - verify clean shutdown and confirm that no private source file enters the
    local frontend build.
- Phase 1 implementation:
  - added a `local` backend runtime profile with configurable application name,
    static-site root, cookie security, cookie same-site policy, origin list,
    and database URL;
  - the local backend serves only `frontend/dist`, never the repository root;
  - added safe single-page fallback routing so `/`, `/portal`, `/campaigns`,
    and other React routes work when opened or refreshed directly;
  - added `scripts/drago-table`, which creates an owner-only local
    configuration, initializes a dedicated loopback-only PostgreSQL cluster,
    builds the frontend, applies Alembic migrations, starts the application,
    health-checks it, prevents idle sleep, opens the DM screen, and stops both
    services cleanly;
  - added the double-click Mac entry point `Start Drago Table.command`;
  - local runtime data lives under
    `~/Library/Application Support/Drago Table/`, outside the repository and
    frontend build;
  - added `docs/local-host.md` with first-start, testing, data-boundary, and
    shutdown instructions;
  - added same-origin local player navigation at `/portal` while retaining the
    existing public Classic URL for the VPS profile;
  - changed visible application naming to Drago Table with Dungeon Master and
    Player roles;
  - replaced the permanent DM/player sidebars with responsive horizontal
    application menus; campaign-specific Table, Map, Character, Party,
    Journal, Handouts, and Rules tabs remain inside campaign workspaces;
  - local authentication cookies are HTTP-only, same-site, and usable on
    loopback HTTP; VPS cookies remain secure by default.
- Verification completed:
  - frontend tests: 78 passed;
  - complete backend/content suite: 154 passed;
  - production frontend build passed;
  - a fresh isolated PostgreSQL cluster initialized and migrated through
    `0020_monster_manual_catalog`;
  - local `/api/health`, DM `/`, and player `/portal` returned HTTP 200 from
    the same origin;
  - DM login set an HTTP-only, same-site local cookie without the HTTPS-only
    flag;
  - the frontend build contains no private rulebook filename or
    `private-reference` path;
  - private-reference source requests return HTTP 404 and a repository-path
    probe returns only the SPA shell rather than repository content;
  - browser-tested DM and player menus at desktop width with no page or menu
    overflow;
  - browser-tested the player interface at 768 pixels with no horizontal
    overflow;
  - verified a local player can be created and authenticated;
  - verified restart uses the existing database and applies no duplicate
    migrations;
  - verified Control-C shuts down Uvicorn and the dedicated PostgreSQL process
    and now returns a successful exit status.
- Deployment status: no VPS deployment was performed or is required for this
  local-host implementation.

## Desktop application launcher correction

- User feedback correctly identified that the repository-level `.command`
  launcher is not the expected desktop application experience.
- Objective: provide a normal `Drago Table.app` with a Drago icon that can be
  installed on the Mac Desktop and opened like another application.
- The desktop application remains a launcher for the tested local host; it does
  not duplicate the React, FastAPI, or PostgreSQL runtime inside a second code
  base.
- Planned verification:
  - generate the application bundle from committed packaging sources;
  - verify its bundle metadata, executable permissions, repository target, and
    icon;
  - install it on the Desktop;
  - launch it and confirm that the local health endpoint becomes available;
  - stop the test instance cleanly.
- Completed:
  - added committed macOS bundle metadata and launcher sources under
    `packaging/macos/`;
  - added `scripts/install-drago-table-app`, including generation of a native
    `.icns` application icon from the Drago source artwork;
  - installed `/Users/ChanceRusso/Desktop/Drago Table.app`;
  - verified the property list, bundle identifier, executable permission,
    generated icon, and exact repository launcher target;
  - updated the local-host instructions to make the Desktop application the
    normal startup path;
  - the first interactive launch remains for the user because it asks the user
    to choose the real local Dungeon Master password.
- First real Desktop launch exposed two defects:
  - the desktop bundle still opened the repository `.command` file in Terminal
    rather than hosting quietly as a normal application;
  - PostgreSQL failed because the `pg_ctl -o` socket-directory option did not
    quote the spaces in `~/Library/Application Support/Drago Table/`.
- Corrections:
  - quote the PostgreSQL socket directory passed through `pg_ctl`;
  - run the local host as a child of `Drago Table.app` with output written to
    the private Application Support log instead of a visible Terminal;
  - show first-time password setup and startup errors through normal macOS
    dialogs;
  - forward application termination to the launcher so quitting the app stops
    FastAPI and PostgreSQL cleanly;
  - update the user instructions to use the Dock application lifecycle.
- Finder launch verification then exposed a Node optional-dependency
  architecture mismatch while rebuilding the frontend. Ordinary application
  startup should not run a developer build, so the Desktop app now starts the
  last verified local build with `--skip-build`. Application changes continue
  to run the tested build step before the user opens the app.
- The following no-build Finder launch exposed the underlying architecture
  mismatch directly: Launch Services ran the script under Intel/Rosetta while
  the Mac's Homebrew PostgreSQL and Python native modules are Apple Silicon.
  The bundle now declares Apple Silicon priority and explicitly starts the
  local host through `/usr/bin/arch -arm64`.
- Final Desktop verification passed:
  - launched `/Users/ChanceRusso/Desktop/Drago Table.app` through Finder/Launch
    Services with no Terminal host window;
  - dedicated PostgreSQL started from the real Application Support directory;
  - migrations completed against the persistent local database;
  - FastAPI and the built frontend started at `http://127.0.0.1:8010`;
  - `/api/health` returned `{"ok":true}`;
  - the app remains running for the user's immediate browser test.
- That shell-bundle verification was not sufficient: Launch Services stopped
  the shell-owned process group shortly after the health check, so the local
  host did not remain available.
- Final packaging decision: replace the shell executable with a compiled,
  native Apple Silicon AppKit application. The native control window:
  - owns the local-host process for the full application lifetime;
  - displays Starting, Ready, or failure state;
  - provides Open Dungeon Master, Open Player View, and Stop Drago Table;
  - provides native first-time password fields;
  - writes service output only to the private Application Support log;
  - terminates the local host cleanly when the window or application quits.

## Interface identity and character-sheet cleanup

- Objective: replace the brown/parchment-heavy presentation with a distinct
  Drago Table identity and correct the broken Character Sheet entry layout.
- Requested scope:
  - replace every temporary circular `DRG` mark with the supplied Drago
    dragon-and-die logo, including login and application headers;
  - add the Drago icon as the browser favicon and distinguish the DM and Player
    document titles;
  - remove the redundant `DRG 1e Character Vault` and descriptive block above
    character sheets;
  - keep `Character Sheet` on one line;
  - ensure a character sheet opened from a portal preserves the originating
    workspace instead of trapping the user;
  - stop ordinary player-side navigation from opening unnecessary new tabs;
  - establish a cleaner palette based on cabernet, vivid dragon red, ivory,
    cool charcoal, and a restrained blue interactive accent.
- Branch: `codex/first-edition-mapping`.
- Baseline commit: `8b564431a08d00a1497819085bf69124c3289cde`.
- Upstream state after `git fetch origin --prune`: local is three commits ahead
  of `origin/codex/first-edition-mapping` and not behind.
- Pre-existing changes: the in-progress native Drago Table launcher corrections
  in `docs/local-host.md`, this work log, `packaging/macos/`, and
  `scripts/drago-table` / `scripts/install-drago-table-app`. They remain in
  scope and must not be lost or silently mixed with unrelated work.
- Planned verification: focused navigation/branding tests, the complete
  frontend test suite, production build, built-asset privacy scan, and visual
  checks of login, DM, Player, and Character Sheet pages in the local app.
- Completed:
  - replaced the temporary `DRG` circles in both application headers and both
    login screens with the canonical Drago dragon-and-die artwork;
  - confirmed the supplied logo, repository source logo, and frontend copy are
    byte-for-byte identical;
  - added the Drago logo favicon and Apple touch icon to the Vite application;
  - retained route-aware browser titles for Dungeon Master and Player views
    and added character-specific Character Sheet titles;
  - removed the redundant Character Vault eyebrow and descriptive copy from
    the Character Sheet page;
  - added a compact, single-line Character Sheet header;
  - DM and Player character-sheet links open a separate tab and carry a safe
    return location back to the campaign or portal;
  - ordinary Player header navigation now stays in the current tab instead of
    producing a new tab on every selection;
  - Player-side local character routes now retain player authentication and
    permissions;
  - edits remain in the current tab while dedicated sheets remain separate;
  - replaced the dominant brown/gold shell with cabernet, dragon red, cool
    charcoal, ivory, blue focus, and semantic green treatments across shared
    headers, panels, actions, tabs, login, DM, and Player surfaces;
  - moved Character Vault cards and controls from yellowed parchment toward a
    cleaner ivory/cabernet/blue presentation.
- Verification:
  - frontend tests: 79 passed;
  - Vite production build passed;
  - built favicon, logo, updated Character Sheet header, return navigation,
    and route-specific title code are present;
  - built output contains no private rulebook filenames or private-reference
    source path;
  - `git diff --check` passed.
- Test handoff: the rebuilt files are served directly by the running local
  Drago Table application. User visual acceptance is pending; no VPS deployment
  is involved.
- User testing exposed a local-DM routing regression: Character View opened the
  correct First Edition URL, but the unified local React router did not yet
  register the DM-side `/1e/characters/:id` and edit routes. The SPA therefore
  rendered no character tool even though the record remained intact.
- Added authenticated local-DM routes for the character index, new character,
  character sheet, and character edit screens. Added a regression test covering
  the sheet and edit routes.
- Post-fix verification: 80 frontend tests passed, the Vite production build
  passed, and `git diff --check` passed.
- User testing then exposed the equivalent local Player-route omission for
  Mapping Mode. Player map cards still generated Classic-host paths, while the
  combined local app needs `/portal/campaigns/:id/maps/:mapId`; that local route
  had not been registered.
- Added one shared player-map path helper, changed both active-map and saved-map
  links to use it, corrected the Mapper's Campaign Home link, and registered
  the protected local Player map route.
- Post-mapper-fix verification: 81 frontend tests passed, the Vite production
  build passed, and `git diff --check` passed.
- User accepted the local application and refreshed interface as the stopping
  point for today. Visual refinements remain for the next session. Final state
  is being committed locally; nothing was pushed and no VPS deployment was
  performed.

## First Edition reference formatting and source labels

- Reviewed `PROGRESS.md`, the 2026-07-22 work log, the deployment standard,
  and the latest deployment record before changing the reference interface.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline commit:
  `8638c8909ed63d14a54d14a50313ff773eab4418`.
- Pre-existing local changes: none.
- Production status of migration `0019_phb_spell_mechanics` remains unconfirmed
  in this log because the final VPS migration/health output has not yet been
  supplied.
- Diagnosed the formatting defect shown on the Spells and Equipment pages:
  the shared First Edition markdown renderer emitted every physical source
  line as an independent paragraph. Markdown source wrapped for readability
  therefore rendered as short, stacked lines.
- Corrected the shared renderer to join consecutive prose lines until a real
  Markdown block boundary. This applies consistently across the complete First
  Edition reference library rather than patching individual pages.
- Added player-facing source-label normalization:
  - official, already verified PHB labels remain unchanged;
  - legacy material is presented as a draft or pending official verification;
  - `OSRIC Reference` is presented as `Player's Guide`;
  - raw OSRIC labels are no longer rendered anywhere in the First Edition
    Markdown library.
- Replaced remaining player-facing OSRIC language in the Dragolance reference
  and general Rules & Settings interface with First Edition / Player's Guide
  language.
- The current DM monster source is now honestly labelled
  `Legacy Monster Catalog — Monster Manual Import Pending`. The underlying
  source key remains unchanged for compatibility until the Monster Manual
  migration is built.
- Important scope boundary: Monster Manual records have **not** yet been
  imported. This formatting release does not relabel the legacy records as
  Monster Manual records and does not change the monster database.
- Verification:
  - visually inspected the local Spells, Equipment, and Combat routes;
  - paragraph flow now spans the available page width;
  - all 400+ First Edition Markdown files render without a visible OSRIC label;
  - frontend tests: 75 passed;
  - production frontend build passed with `index-BLAxLJe8.js` and
    `index-6iuCmQ6K.css`.

## Resume checkpoint — Monster Manual catalog

- Objective: replace the legacy OSRIC core monster catalog with a
  DM-only Monster Manual catalog suitable for encounter selection and the DM
  combat table.
- Intended scope:
  - audit the private Monster Manual scan and define a reproducible import;
  - preserve structured printed statistics and concise operational mechanics;
  - make `Monster Manual` the default core monster source in DM tools;
  - retain campaign/adventure-specific monsters;
  - archive superseded legacy core rows without breaking saved references;
  - verify that no private PDF or extracted source pages enter a frontend build,
    API response, commit, or deployment.
- Branch: `codex/first-edition-mapping`.
- Baseline commit:
  `507b6eb7b81a79f9de0683be6f944c86a7afe64d`.
- Upstream state after `git fetch origin --prune`: local HEAD exactly matches
  `origin/codex/first-edition-mapping`.
- Pre-existing local changes: none.
- Proven production baseline remains
  `0b6f26c24c071ed9d977ba259fcfcf0376f63c2c` at migration
  `0018_archive_legacy_spells`. The later spell-mechanics and reference-format
  commits are pushed but are not yet proven deployed by a deployment record.
- Planned verification:
  - compare entry names and printed statistics against rendered source pages;
  - validate source/page/verification metadata and unique slugs;
  - run focused backend catalog and DM-tool tests;
  - run the complete frontend tests and production build;
  - inspect the built output for private-source filenames or extracted text;
  - do not begin VPS deployment until the deployment runbook and inventory are
    reread and the exact pushed commit is confirmed.
- Provenance decision: existing legacy rows may be updated in place when their
  structured statistics match the Monster Manual. This preserves stable slugs
  and avoids duplicates. `Monster Manual` will be assigned only after
  field-by-field verification; partially checked or unmatched OSRIC-derived
  rows retain an explicit legacy source.
- Completed the first full structural comparison of the 284 legacy records
  against the private 114-page Monster Manual scan:
  - detected 217 independently structured main-entry stat blocks;
  - found 126 direct name matches;
  - found 158 legacy records without a direct Monster Manual main-entry name;
  - found 93 Monster Manual main entries without a direct legacy match;
  - all 126 direct matches differ in at least one literal field and therefore
    none can be safely bulk-relabeled without normalization or correction.
- The differences are not merely typography. The audit exposed combined
  variant rows, expanded treasure tables in place of printed treasure-type
  codes, notation conversions, later-book monsters, and crossed/corrupt legacy
  fields. Example: the legacy Basilisk record contains an unrelated attack
  description and incorrect frequency/hit dice compared with the Monster
  Manual.
- Added `tools/audit_monster_manual.py` and generated
  `docs/sources/monster-manual-legacy-audit.json`. The report contains
  structured statistics and comparisons only; it excludes narrative source
  text, page images, and the private PDF path.
- Visually verified extraction structure against rendered pages near the
  beginning, middle, and end of the Monster Manual.
- Review conclusion: keep stable rows and slugs where possible, but do not
  change the current 284-record catalog source en masse. Matching records must
  be corrected from the Monster Manual; legacy-only records remain legacy, and
  missing Monster Manual entries must be added separately.
- Adventure-source rule confirmed: adventure-specific monsters keep their
  adventure title as source and coexist with the core catalog. Current and
  future adventure imports are never relabeled as Monster Manual or legacy
  OSRIC merely because a name overlaps.

## Monster Manual catalog revision

- Generated a backend-only catalog of 217 Monster Manual main entries with
  printed statistics and PDF-page provenance.
- The catalog is stored under `backend/app/data/`, outside the frontend's
  public content tree.
- Retained OSRIC prose is reduced to operational sentences about attacks,
  saves, special abilities, immunities, encounter composition, and other
  table mechanics. It is identified separately as `OSRIC operational notes`;
  ecology and descriptive filler are omitted.
- Added migration `0020_monster_manual_catalog`:
  - matching slugs are updated in place;
  - missing Monster Manual main entries are inserted;
  - unmatched core records become `Legacy OSRIC Catalog`;
  - adventure-specific records retain their adventure source and receive
    `adventure_source` verification metadata;
  - primary and supplemental provenance are stored independently.
- Updated the DM monster API payload with verification and supplemental-source
  metadata.
- Restricted `/api/1e/monsters` to DM/admin authentication. Players can no
  longer call the monster catalog API directly.
- Updated Drago Table to default to `Monster Manual`, sort it first, and show
  supplemental provenance where applicable.
- Added a frontend build safeguard that excludes:
  - the legacy core monster JSON;
  - adventure `monsters.json` files;
  - development content tools.
  The built frontend contains no Monster Manual catalog, adventure monster
  catalog, private rulebook PDF, or private Monster Manual filename.
- Migration verification:
  - fresh database upgraded through `0020`;
  - seeding produced 422 unique slugs: 217 Monster Manual, 160 retained legacy,
    and 45 N1 adventure records;
  - production-like `0019` database with an existing Basilisk and adventure
    monster upgraded successfully;
  - Basilisk was corrected in place;
  - the adventure record remained unchanged under its adventure source.
- Verification:
  - focused Monster Manual tests: 4 passed;
  - complete backend suite: 152 passed;
  - frontend suite: 75 passed;
  - production frontend build passed with `index-D5MBrG76.js` and
    `index-6iuCmQ6K.css`;
  - Python and JSON structural validation passed;
  - private-source and DM-only monster build leak checks passed.
- Deployment has not started. Production remains at the previously proven
  application SHA and migration `0018` until this work is committed, pushed,
  backed up, and deployed through the runbook.
- Local implementation commit:
  `64ae530` (`Import verified Monster Manual catalog`).
- Log commit `e45bdbc` and implementation commit `64ae530` were pushed to
  `origin/codex/first-edition-mapping`. This push is not a production
  deployment.

## Monster Manual production deployment

- Began deployment from proven production application SHA `507b6eb` and
  migration `0018_archive_legacy_spells`.
- Created and verified PostgreSQL, DM web-root, Classic web-root, and generated
  package-lock backups before switching the checkout.
- Server checkout advanced cleanly to deployment candidate `5cd6a8c`.
- VPS focused Monster Manual tests, all 75 frontend tests, production build,
  and private/DM-only artifact exclusions passed.
- The full VPS backend suite produced 150 passes and two environment-only
  failures: a test hardcoded to macOS `/private/tmp` and a coverage test for a
  private Dragonlance PDF intentionally excluded from production.
- First `0019` migration attempt failed transactionally at the Identify spell:
  its `area_of_effect` contained the following Find Familiar table and exceeded
  the PostgreSQL `varchar(160)` column. Production remained at `0018`.
- Corrected Identify to the printed compact header value `One item`, added a
  parser override to prevent recurrence, and added regression tests enforcing
  database lengths for every compact spell-header field.
- Committed and pushed the correction as `9c12ea3`.
- Production migration retry succeeded through
  `0020_monster_manual_catalog`.
- Restarted `russo-backend.service`; internal, DM-public, and Classic-public
  health checks returned `{"ok":true}`.
- Deployed the sanitized frontend build explicitly to both web roots:
  - `/var/www/dm.dragorussogames.com/`;
  - `/var/www/classic.dragorussogames.com/`.
- Production verification:
  - exact application SHA `9c12ea3ca4ada443bceae921a5bd1d1cd7597d85`;
  - 217 Monster Manual, 160 retained legacy, and 45 N1 adventure records;
  - player and unauthenticated monster API calls return HTTP 401;
  - authenticated DM/admin call returns 217 Monster Manual records;
  - served roots contain no private PDF, static core/adventure monster JSON, or
    development tools;
  - Basilisk has the corrected Monster Manual statistics;
  - no backend error-level journal entries appeared after restart.
- Rollback was not required.

## Player-side completion backlog and Monster XP checkpoint

- Objective: record the remaining player-side work in one ordered backlog and
  complete it one independently verified item at a time.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline commit:
  `6b1d48d867e7139e0f79569def034e6eb420a2ca`.
- Upstream state after `git fetch origin --prune`: local HEAD exactly matches
  `origin/codex/first-edition-mapping`.
- Pre-existing local changes: none.
- Ordered backlog:
  1. populate and verify every Monster Manual `LEVEL/X.P. VALUE`;
  2. verify the deployed Mapper right-click behavior ends the current connected
     wall while leaving Wall selected, and add regression coverage;
  3. finish Mapping Mode levels/floors, zoom and navigation, revision
     restoration, conflict-safe synchronization, and the authenticated
     DM/Mapper/player smoke test;
  4. verify durable multiplayer Combat Mode state, including tokens, colors,
     grid changes, HP, reconnects, and simultaneous updates;
  5. implement player character multi-class functionality;
  6. implement player character dual-class functionality, including inactive
     former-class restrictions and reactivation rules;
  7. finish Player's Handbook verification for spell mechanics, character
     creation, progression tables, remaining legacy rules, equipment gaps,
     armour movement limits, and non-weapon weights;
  8. remove or replace remaining visible OSRIC labels;
  9. build Hex Crawl Mode with persistent maps, terrain, routes, landmarks,
     notes, and wilderness time;
  10. build DM-controlled player-safe campaign handouts;
  11. complete responsive, accessibility, error-state, reconnect, and full
      new-player-journey review.
- Current item: Monster Manual XP completion. Diagnosis confirms that all 217
  imported Monster Manual records were generated with `level_xp: null`; this
  is missing source data rather than a frontend display defect.
- Source correction from visual PDF review:
  - the original Monster Manual stat blocks do not contain a printed
    `LEVEL/X.P. VALUE` field;
  - the controlling standard values are in Dungeon Master's Guide Appendix E,
    `Alphabetical Recapitulation of Monsters (With Experience Point Values)`;
  - the site must identify Monster Manual as the monster-stat source and the
    Dungeon Master's Guide as the XP source rather than falsely attributing XP
    to the Monster Manual.
- Planned verification for the current item:
  - extract the standard XP formulas reproducibly from DMG Appendix E;
  - review exceptions and variant entries against the source;
  - keep private source pages and narrative text out of commits and builds;
  - migrate existing Monster Manual rows without changing adventure sources;
  - require each Monster Manual record with a corresponding DMG Appendix E row
    to have a nonblank XP formula and explicit DMG provenance;
  - run focused catalog/migration tests, the complete backend suite, frontend
    tests, and the production-build private-source exclusion checks.

## Mapping Mode group-test readiness

- Objective: defer Monster XP and make Mapping Mode complete enough for a live
  group test before beginning Hex Crawl Mode.
- Baseline branch: `codex/first-edition-mapping`.
- Baseline commit:
  `e4c1746beb42ee521d51f46dff6a2f97d58941a2`.
- Upstream state after `git fetch origin --prune`: local HEAD exactly matches
  `origin/codex/first-edition-mapping`.
- Pre-existing changes: none. The untracked `tmp/` directory reported during
  preflight contained only PDF-review images created during the previous
  Monster XP source check and was removed before this work began.
- Revised work order:
  1. verify and harden right-click wall-chain termination while leaving Wall
     selected;
  2. complete floors/levels, zoom and navigation, revision recovery, and safe
     shared-map updates;
  3. exercise all tools and permissions in authenticated DM, Mapper, and player
     views;
  4. deploy and conduct the live group test;
  5. begin Greyhawk Hex Crawl Mode on the supplied Darlene map;
  6. wait for a separate user-supplied Dragonlance map before enabling a
     Dragonlance Hex Crawl base map.
- “Combat synchronization” is deferred. It means keeping DM and player combat
  views consistent for mode, grid, token positions and colors, hit points, and
  reconnects; it is not part of this Mapping Mode readiness pass.
- Supplied Greyhawk map:
  - source filename: `World of Greyhawk, Darlene.jpg`;
  - dimensions: 13,202 × 10,268 pixels;
  - source size: approximately 23 MB;
  - SHA-256:
    `8637dabdba1552cc56ee774995a3a5548defd9cdcf549ba4fa278c5d4f86167c`;
  - intended use: player-facing Greyhawk Hex Crawl base map;
  - source notice visible on the image identifies it as an unofficial fan
    content digital map.
- Planned verification:
  - add interaction-level Mapper tests, not only coordinate helper tests;
  - verify persistence and authorization through backend API tests;
  - run complete frontend and backend suites and a production build;
  - confirm the Greyhawk source asset is deliberately included only in the
    player-facing Hex Crawl asset path;
  - complete browser smoke tests with separate DM, Mapper, and player sessions.
- Mapping readiness implementation:
  - right-click now terminates only the current connected wall chain and leaves
    Wall selected; Escape provides the same reset without changing tools;
  - legacy single-floor drawings upgrade in memory to a versioned multi-floor
    structure without losing objects or notes;
  - the Mapper can create, select, rename, and delete separately persisted
    floors, while viewers follow the saved active floor;
  - zoom supports 50%-200%, keeps the same visual center while changing scale,
    persists in the shared viewport, and remains read-only for viewers;
  - the existing revision snapshots now have a Mapper history interface and a
    safe restore action;
  - Mapper saves carry an expected revision, so an older window receives a
    conflict instead of overwriting newer work; the interface offers an
    explicit reload of the latest saved map;
  - backend validation now accepts and bounds multi-floor map documents while
    retaining compatibility with existing single-floor maps.
- Greyhawk asset:
  - copied the exact supplied image to
    `frontend/player-assets/maps/greyhawk/world-of-greyhawk-darlene.jpg`;
  - the repository copy matches the supplied SHA-256 and original
    13,202 × 10,268 dimensions.
  - deployment review caught that a shared Vite public asset would also land in
    the DM root; the image was moved outside Vite's shared build, removed from
    the DM root, and retained as an explicit Classic-only deployment asset.
- Verification completed so far:
  - frontend tests: 77 passed;
  - focused campaign mapping backend test passed;
  - complete backend suite: 154 passed;
  - production frontend build passed;
  - browser-confirmed that right-click removes the active wall endpoint,
    leaves Wall selected, and the next left-click starts a new wall elsewhere;
  - browser-confirmed separate floor creation, active-floor display, saved
    125% zoom, and a populated recoverable revision-history panel.
  - browser-confirmed that an ordinary campaign member receives the Mapper's
    saved `Lower Vault` floor and 125% viewport with floor and zoom controls
    disabled;
  - browser-confirmed square-centered pit placement, numbered-note placement,
    note-ledger editing, and note autosave;
  - final frontend tests: 77 passed;
  - final production frontend build passed with `index-ZxKDv_jM.js` and
    `index-CB3Av2ae.css`;
  - build scan contains no private-reference path or private rulebook
    filename.
- Production deployment completed:
  - deployed and verified
    `1761590b539caa758394ff77373b79d1ba0307a4` from
    `codex/first-edition-mapping`;
  - preserved database and both web roots in timestamped backups before
    mutation;
  - VPS frontend tests passed 77/77, the focused mapping backend test passed,
    and the production frontend build passed;
  - database migration remains `0020_monster_manual_catalog`;
  - restarted `russo-backend.service`; internal, DM, and Classic health checks
    passed;
  - deployed the shared application to DM and Classic, then copied
    `frontend/player-assets/` only to Classic;
  - verified the Greyhawk JPEG is available on Classic and absent from DM;
  - verified neither web root contains a private 1e rulebook filename or
    `private-reference` directory;
  - verified the public private-reference probe returns only the application
    HTML shell and unauthenticated map API access returns 401;
  - no rollback was required.
